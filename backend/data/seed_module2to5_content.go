package data

import (
	"golanger/backend/models"
	"gorm.io/gorm"
)

func SeedModule2to5Content(db *gorm.DB) {
	lessons := map[string]string{

		// ── Спринт 3. IT-команда и Git ──

		"3-1-sdlc": `# Жизненный цикл программного обеспечения (SDLC)

Прежде чем писать код, важно понять: как вообще создаётся программный продукт? От идеи до работающего приложения — путь состоит из нескольких этапов. Этот путь называется **SDLC** (Software Development Life Cycle).

---

## Этапы SDLC

### 1. Идея (Concept)

Всё начинается с проблемы. Кто-то замечает: «Людям неудобно записывать задачи в блокнот — давайте сделаем приложение». Идея может прийти от бизнеса, от пользователей или от самих разработчиков.

**Пример:** «Сделаем сервис прогноза погоды с кешированием, чтобы не дёргать внешний API на каждый запрос».

### 2. Сбор требований (Requirements)

На этом этапе команда выясняет, **что именно** должна делать программа. Записываются функциональные требования (что делает) и нефункциональные (как быстро, насколько безопасно).

| Тип требования | Пример |
|----------------|--------|
| Функциональное | Пользователь может создать задачу |
| Функциональное | API возвращает прогноз по городу |
| Нефункциональное | Ответ сервера < 200 мс |
| Нефункциональное | Данные хранятся в PostgreSQL |

### 3. Проектирование (Design)

Архитектор или тимлид решает **как** это будет устроено: какие сервисы, какие базы данных, какие API. Рисуются схемы, выбираются технологии.

### 4. Разработка (Development)

Программисты пишут код. Каждая задача оформляется как Issue, берётся в спринт, реализуется в отдельной ветке Git и проходит код-ревью.

### 5. Тестирование (Testing)

QA-инженеры проверяют, что всё работает: ручные тесты, автоматические тесты, нагрузочное тестирование. Найденные ошибки возвращаются разработчикам.

### 6. Деплой (Deployment)

Готовый код выкатывается на сервер. Пользователи получают доступ к новой версии. Используются Docker, CI/CD пайплайны, Kubernetes.

### 7. Поддержка (Maintenance)

После запуска нужно следить за работоспособностью: мониторинг, исправление багов, обновления, добавление новых функций.

---

## Схема SDLC

` + "```" + `
Идея → Требования → Проектирование → Разработка → Тестирование → Деплой → Поддержка
  ↑                                                                            |
  └────────────────────────────────────────────────────────────────────────────┘
                              (цикл повторяется)
` + "```" + `

Процесс **циклический** — после поддержки появляются новые идеи и улучшения, и цикл повторяется.

---

## Зачем это знать разработчику

- Ты не просто «пишешь код» — ты участвуешь в процессе создания продукта
- Понимание SDLC помогает общаться с командой на одном языке
- На собеседованиях часто спрашивают про SDLC и Agile
- Ты будешь лучше понимать, почему код проходит ревью, зачем нужны тесты и что такое деплой

На этом курсе ты пройдёшь все этапы: от требований к проекту до деплоя в Docker.
`,

		"3-2-features-bugs": `# Фичи и баги

В разработке есть два основных типа задач: **фичи** (новые возможности) и **баги** (ошибки). Разберём, что это и как они попадают в работу.

---

## Что такое фича (Feature)

Фича — это **новая функциональность**, которую нужно добавить в продукт.

**Примеры фич:**
- Добавить поиск по задачам
- Реализовать кеширование запросов к Weather API
- Добавить авторизацию через JWT-токены
- Показывать количество оставшихся попыток в игре

Фича начинается с потребности пользователя или бизнеса. Продакт-менеджер описывает что нужно, а разработчик — реализует.

---

## Что такое баг (Bug)

Баг — это **ошибка** в программе, когда поведение отличается от ожидаемого.

**Примеры багов:**
- Сервер возвращает 500 вместо списка задач
- Игра принимает отрицательные числа как ввод
- Кеш не очищается после истечения TTL
- При пустом городе API падает с паникой

---

## Как они попадают в бэклог

**Бэклог** — это список всех задач, которые нужно сделать. Он живёт в трекере задач (GitHub Issues, Jira, Linear).

` + "```" + `
Источник задачи                  Бэклог                    Спринт

Пользователь: "Хочу фильтр" →  [Фича: фильтр задач]  →  Берём в работу
QA: "Нашёл ошибку"           →  [Баг: 500 на /tasks]  →  Берём в работу
Разработчик: "Нужен рефакт"  →  [Tech debt: рефакт]   →  Планируем позже
PM: "Бизнес хочет отчёты"    →  [Фича: отчёты]        →  Планируем позже
` + "```" + `

---

## Приоритеты

Не все задачи одинаково важны:

| Приоритет | Описание | Пример |
|-----------|----------|--------|
| Critical | Продукт не работает | Сервер не запускается |
| High | Основная функция сломана | Нельзя создать задачу |
| Medium | Неудобство, но работает | Неправильная сортировка |
| Low | Мелочь, косметика | Опечатка в тексте |

Баги с приоритетом Critical исправляются немедленно. Фичи планируются на спринты.

---

## Жизненный цикл задачи

` + "```" + `
Open → In Progress → In Review → Testing → Done
` + "```" + `

1. **Open** — задача создана, ждёт исполнителя
2. **In Progress** — разработчик пишет код
3. **In Review** — код на ревью у коллеги
4. **Testing** — QA проверяет
5. **Done** — задача закрыта, код в продакшене

На этом курсе ты будешь создавать Issues для своих проектов — как фичи, так и баги.
`,

		"3-3-team-roles": `# Роли в IT-команде

Программный продукт создаёт не один человек, а команда. У каждого своя роль и зона ответственности.

---

## Основные роли

### PM (Project Manager / Product Manager)

**Что делает:** определяет, что и зачем строим. Собирает требования от бизнеса и пользователей, расставляет приоритеты, следит за сроками.

**Простыми словами:** решает «ЧТО делаем и ЗАЧЕМ».

### TL (Tech Lead / Team Lead)

**Что делает:** отвечает за техническую часть. Выбирает архитектуру, проводит код-ревью, помогает команде с техническими решениями.

**Простыми словами:** решает «КАК делаем».

### Dev (Developer / Разработчик)

**Что делает:** пишет код. Реализует фичи, исправляет баги, пишет тесты, участвует в код-ревью.

**Простыми словами:** «ДЕЛАЕТ».

### QA (Quality Assurance / Тестировщик)

**Что делает:** проверяет качество. Пишет тест-кейсы, тестирует новые фичи, находит баги, проверяет исправления.

**Простыми словами:** «ПРОВЕРЯЕТ».

### DevOps

**Что делает:** настраивает инфраструктуру. CI/CD пайплайны, Docker, Kubernetes, мониторинг, деплой.

**Простыми словами:** «ДОСТАВЛЯЕТ код на сервер».

---

## Таблица ролей

| Роль | Основная задача | Инструменты |
|------|----------------|-------------|
| PM | Требования, приоритеты | Jira, Notion, Figma |
| TL | Архитектура, код-ревью | GitHub, IDE, доска |
| Dev | Написание кода | VS Code, Go, Git |
| QA | Тестирование | Postman, Selenium, тест-кейсы |
| DevOps | Инфраструктура, деплой | Docker, K8s, CI/CD, Terraform |

---

## Дополнительные роли

| Роль | Что делает |
|------|-----------|
| **Designer (UX/UI)** | Проектирует интерфейс |
| **BA (Business Analyst)** | Переводит бизнес-требования в технические |
| **SRE (Site Reliability Engineer)** | Следит за надёжностью в продакшене |
| **DBA** | Управляет базами данных |

---

## Как выглядит типичный день

` + "```" + `
10:00  Стендап — каждый рассказывает что сделал и что будет делать
10:15  Dev берёт задачу из спринта, пишет код
12:00  TL проводит код-ревью пулл-реквеста
14:00  QA тестирует готовую фичу
15:00  DevOps деплоит на staging
16:00  PM показывает демо заказчику
` + "```" + `

На этом курсе ты в роли **Dev** — пишешь код, создаёшь коммиты, оформляешь пулл-реквесты. Но понимание других ролей поможет тебе работать в команде.
`,

		"3-4-methodologies": `# Методологии разработки

Как команда организует свою работу? Для этого существуют методологии — набор правил и практик.

---

## Waterfall (водопад)

Старый подход: все этапы идут последовательно.

` + "```" + `
Требования → Дизайн → Разработка → Тестирование → Деплой
` + "```" + `

**Проблема:** если на этапе тестирования выяснилось, что требования были неправильными — нужно начинать сначала. Дорого и долго.

---

## Agile (гибкая методология)

Agile — это **философия**, а не конкретный процесс. Основная идея: делаем продукт **маленькими итерациями**, получаем обратную связь и адаптируемся.

**Ключевые принципы Agile:**
- Люди важнее процессов
- Работающий продукт важнее документации
- Сотрудничество с заказчиком важнее контракта
- Готовность к изменениям важнее следования плану

---

## Scrum

Scrum — самая популярная реализация Agile. Работа делится на **спринты** — короткие итерации (обычно 1–2 недели).

### Артефакты Scrum

| Артефакт | Что это |
|----------|---------|
| Product Backlog | Все задачи продукта |
| Sprint Backlog | Задачи на текущий спринт |
| Increment | Готовый результат спринта |

### Церемонии Scrum

| Церемония | Когда | Зачем |
|-----------|-------|-------|
| **Sprint Planning** | Начало спринта | Выбираем задачи на спринт |
| **Daily Standup** | Каждый день, 15 минут | Синхронизация команды |
| **Sprint Review** | Конец спринта | Показываем результат |
| **Retrospective** | Конец спринта | Обсуждаем что улучшить |

### Стендап (Daily Standup)

Каждый участник отвечает на 3 вопроса:
1. Что я сделал вчера?
2. Что буду делать сегодня?
3. Есть ли блокеры?

Стендап длится **не более 15 минут**. Это не обсуждение — это синхронизация.

---

## Kanban

Kanban — более простой подход. Нет спринтов — задачи текут непрерывно по доске.

` + "```" + `
| To Do      | In Progress | In Review | Done       |
|------------|-------------|-----------|------------|
| Фича: кеш  | Баг: 500    | Фича: API |            |
| Фича: auth |             |           | Фича: CRUD |
` + "```" + `

**Правила Kanban:**
- Ограничение WIP (Work In Progress) — не больше N задач в колонке
- Задача двигается слева направо
- Визуализация процесса на доске

---

## Сравнение

| | Scrum | Kanban |
|---|---|---|
| Итерации | Фиксированные спринты | Непрерывный поток |
| Планирование | В начале спринта | По мере необходимости |
| Роли | Scrum Master, PO, Team | Нет специальных ролей |
| Лучше для | Продуктовой разработки | Поддержки, DevOps |

---

Этот курс построен по принципам Agile: ты работаешь спринтами, у каждого спринта есть цель, а проекты разбиты на маленькие шаги (коммиты).
`,

		"3-5-git-basics": `# Основы Git

Git — система контроля версий. Она сохраняет историю изменений в коде и позволяет работать над проектом в команде.

---

## Зачем нужен Git

- **История изменений** — можно посмотреть, что менялось и когда
- **Откат** — можно вернуться к любой предыдущей версии
- **Командная работа** — несколько человек работают над одним проектом
- **Портфолио** — твои проекты на GitHub видят работодатели

---

## Основные команды

### Инициализация репозитория

` + "```bash" + `
mkdir my-project
cd my-project
git init
` + "```" + `

Команда ` + "`git init`" + ` создаёт скрытую папку ` + "`.git`" + ` — в ней хранится вся история.

### Проверка статуса

` + "```bash" + `
git status
` + "```" + `

Показывает:
- Какие файлы изменены
- Какие добавлены в staging (готовы к коммиту)
- Какие не отслеживаются (новые файлы)

### Добавление файлов в staging

` + "```bash" + `
git add main.go          # один файл
git add .                # все изменённые файлы
git add handlers/ models/ # несколько папок
` + "```" + `

Staging — это «зона подготовки». Ты выбираешь, какие изменения войдут в коммит.

### Создание коммита

` + "```bash" + `
git commit -m "feat: add user registration endpoint"
` + "```" + `

Коммит — это **снимок** состояния проекта. Каждый коммит имеет уникальный хеш, автора и сообщение.

### Просмотр истории

` + "```bash" + `
git log                  # полная история
git log --oneline        # компактный вид
git log --oneline -5     # последние 5 коммитов
` + "```" + `

---

## Правила написания коммитов

Хорошие коммиты — признак профессионала. Используй формат **Conventional Commits**:

` + "```" + `
<тип>: <описание>
` + "```" + `

| Тип | Когда использовать | Пример |
|-----|-------------------|--------|
| feat | Новая функциональность | feat: add weather endpoint |
| fix | Исправление бага | fix: handle empty city parameter |
| refactor | Рефакторинг без изменения поведения | refactor: extract cache logic |
| docs | Документация | docs: add API usage to README |
| chore | Настройка, зависимости | chore: update go.mod dependencies |

### Плохие коммиты

` + "```" + `
git commit -m "fix"
git commit -m "update"
git commit -m "asdfgh"
git commit -m "работает!!!"
` + "```" + `

### Хорошие коммиты

` + "```" + `
git commit -m "feat: add difficulty selection to guessing game"
git commit -m "fix: return 400 for invalid city name"
git commit -m "docs: add installation instructions to README"
` + "```" + `

---

## Полный пример рабочего процесса

` + "```bash" + `
# 1. Создаём проект
mkdir guessing-game && cd guessing-game
git init
go mod init guessing-game

# 2. Пишем код
# (создаём main.go)

# 3. Проверяем статус
git status

# 4. Добавляем и коммитим
git add main.go go.mod
git commit -m "feat: init project with greeting message"

# 5. Проверяем историю
git log --oneline
` + "```" + `

Git будет твоим ежедневным инструментом. Привыкай коммитить часто и с понятными сообщениями.
`,

		"3-6-git-branches": `# Ветки в Git

Ветки (branches) позволяют работать над разными задачами параллельно, не мешая друг другу.

---

## Зачем нужны ветки

Представь: ты работаешь над новой фичей, и тут приходит срочный баг. Без веток пришлось бы откладывать недоделанную фичу. С ветками — просто переключаешься.

` + "```" + `
main ──●──●──●──────────────●── (стабильная версия)
              \            /
feature ───────●──●──●──●── (новая фича)
` + "```" + `

---

## Основные команды

### Создание ветки

` + "```bash" + `
git branch feature/add-cache     # создать ветку
git checkout feature/add-cache   # переключиться на неё

# Или одной командой:
git checkout -b feature/add-cache
` + "```" + `

### Просмотр веток

` + "```bash" + `
git branch          # список локальных веток
git branch -a       # все ветки (включая remote)
` + "```" + `

Текущая ветка отмечена звёздочкой: ` + "`* feature/add-cache`" + `

### Переключение между ветками

` + "```bash" + `
git checkout main              # вернуться на main
git checkout feature/add-cache # переключиться обратно
` + "```" + `

### Слияние (merge)

Когда фича готова, сливаем ветку в main:

` + "```bash" + `
git checkout main                # переходим на main
git merge feature/add-cache      # сливаем фичу в main
` + "```" + `

---

## Схема Feature Branch

Это стандартный рабочий процесс в командах:

` + "```" + `
1. Берёшь задачу из бэклога
2. Создаёшь ветку: git checkout -b feature/task-name
3. Пишешь код, делаешь коммиты
4. Пушишь ветку на GitHub
5. Создаёшь Pull Request
6. Код проходит ревью
7. Ветка сливается в main
` + "```" + `

---

## Именование веток

| Тип | Формат | Пример |
|-----|--------|--------|
| Фича | feature/описание | feature/add-weather-cache |
| Баг | fix/описание | fix/empty-city-error |
| Рефакторинг | refactor/описание | refactor/extract-handlers |

---

## Конфликты слияния

Иногда два разработчика меняют одну и ту же строку. При слиянии возникает **конфликт**:

` + "```" + `
<<<<<<< HEAD
fmt.Println("Hello from main")
=======
fmt.Println("Hello from feature")
>>>>>>> feature/greeting
` + "```" + `

Нужно вручную выбрать правильный вариант, убрать маркеры и закоммитить.

---

## Удаление ветки

После слияния ветка больше не нужна:

` + "```bash" + `
git branch -d feature/add-cache   # удалить локально
` + "```" + `

---

На этом курсе ты будешь использовать ветки в проектах: каждый шаг — отдельная ветка или коммит.
`,

		"3-7-git-remote": `# Удалённые репозитории и GitHub

До сих пор мы работали с Git **локально**. Теперь научимся хранить код в облаке — на **GitHub**.

---

## Что такое GitHub

GitHub — платформа для хранения Git-репозиториев. Здесь живут проекты: Go, Docker, Kubernetes, React и миллионы других.

GitHub — это не только хранилище. Это:
- **Портфолио** — работодатели смотрят твой GitHub
- **Совместная работа** — Issues, Pull Requests, код-ревью
- **CI/CD** — GitHub Actions для автоматизации

---

## Пошаговый пример

### 1. Создай репозиторий на GitHub

1. Зайди на [github.com](https://github.com)
2. Нажми **New repository**
3. Введи имя: ` + "`guessing-game`" + `
4. Выбери **Public** (для портфолио)
5. **Не** добавляй README (мы создадим сами)
6. Нажми **Create repository**

### 2. Свяжи локальный репозиторий с GitHub

` + "```bash" + `
# Если ещё нет локального репо:
mkdir guessing-game && cd guessing-game
git init
go mod init guessing-game
echo \"# Guessing Game\" > README.md
git add .
git commit -m \"feat: init project\"

# Добавляем remote:
git remote add origin https://github.com/username/guessing-game.git
` + "```" + `

### 3. Отправь код на GitHub (push)

` + "```bash" + `
git push -u origin main
` + "```" + `

Флаг ` + "`-u`" + ` устанавливает связь между локальной веткой и удалённой. После этого можно писать просто ` + "`git push`" + `.

### 4. Скачай чужой репозиторий (clone)

` + "```bash" + `
git clone https://github.com/username/some-project.git
cd some-project
` + "```" + `

` + "`clone`" + ` скачивает весь репозиторий со всей историей.

### 5. Получи обновления (pull)

` + "```bash" + `
git pull origin main
` + "```" + `

Скачивает изменения с GitHub и сливает с твоей локальной версией.

---

## Основные команды remote

| Команда | Что делает |
|---------|-----------|
| ` + "`git remote add origin URL`" + ` | Добавить удалённый репозиторий |
| ` + "`git remote -v`" + ` | Посмотреть удалённые репозитории |
| ` + "`git push`" + ` | Отправить коммиты на GitHub |
| ` + "`git pull`" + ` | Получить изменения с GitHub |
| ` + "`git clone URL`" + ` | Скопировать репозиторий |

---

## Типичный рабочий процесс

` + "```bash" + `
# Утро: получаем свежие изменения
git pull

# Работаем: создаём ветку, пишем код
git checkout -b feature/new-endpoint
# ... пишем код ...
git add .
git commit -m \"feat: add new endpoint\"

# Пушим ветку
git push -u origin feature/new-endpoint

# На GitHub: создаём Pull Request
# После ревью: мерджим в main
` + "```" + `

---

## SSH vs HTTPS

Для push на GitHub нужна авторизация:

| Способ | Как настроить |
|--------|--------------|
| HTTPS | Используй Personal Access Token |
| SSH | Сгенерируй ключ: ` + "`ssh-keygen`" + `, добавь на GitHub |

SSH удобнее — не нужно вводить пароль каждый раз.

Теперь твой код хранится в облаке, и работодатели могут его увидеть!
`,

		"3-8-issues": `# GitHub Issues

Issues (задачи) — основной способ трекинга работы на GitHub. Каждая фича и каждый баг оформляются как Issue.

---

## Зачем нужны Issues

- **Планирование** — видно, что нужно сделать
- **Прозрачность** — вся команда знает статус задач
- **История** — можно вернуться и посмотреть, почему приняли решение
- **Портфолио** — показывает, что ты умеешь работать по процессу

---

## Как создать Issue

1. Зайди в репозиторий на GitHub
2. Вкладка **Issues** → **New issue**
3. Заполни заголовок и описание
4. Добавь **Labels** (метки): bug, feature, documentation
5. Назначь **Assignee** (исполнителя)

---

## Acceptance Criteria

**Acceptance Criteria** (критерии приёмки) — список условий, при выполнении которых задача считается готовой.

Без AC задачу легко «додумать» неправильно. AC снимают неоднозначность.

---

## Шаблон фичи

` + "```" + `
### Описание
Как пользователь, я хочу выбирать уровень сложности,
чтобы игра была интереснее.

### Acceptance Criteria
- [ ] При старте игры показывается меню: easy, medium, hard
- [ ] Easy: 10 попыток, Medium: 5 попыток, Hard: 3 попытки
- [ ] При некорректном вводе показывается ошибка
- [ ] После выбора сложности начинается раунд

### Технические детали
- Использовать switch для выбора сложности
- Максимум попыток передаётся в функцию playRound()
` + "```" + `

---

## Шаблон бага

` + "```" + `
### Описание бага
Сервер падает с паникой при пустом параметре city.

### Шаги воспроизведения
1. Запустить сервер: go run main.go
2. Отправить запрос: curl "http://localhost:8080/weather?city="
3. Сервер падает

### Ожидаемое поведение
Сервер возвращает 400 Bad Request с сообщением об ошибке.

### Фактическое поведение
Сервер падает с panic: runtime error: index out of range.

### Окружение
- Go 1.22
- macOS 14
- Браузер: не применимо (curl)
` + "```" + `

---

## Labels (метки)

| Label | Цвет | Когда использовать |
|-------|------|--------------------|
| bug | красный | Ошибка в программе |
| feature | зелёный | Новая функциональность |
| documentation | синий | Документация |
| good first issue | фиолетовый | Простая задача для новичков |
| priority: high | оранжевый | Срочная задача |

---

## Связь Issues и Pull Requests

В описании PR можно написать:

` + "```" + `
Closes #3
` + "```" + `

Когда PR будет смерджен, Issue #3 автоматически закроется. Это стандартная практика.

---

## Практика

В каждом проекте на этом курсе ты будешь:
1. Создавать Issues для каждого шага
2. Писать Acceptance Criteria
3. Связывать PR с Issues

Это не просто формальность — это навык, который ценят работодатели.
`,
	}
	for slug, content := range lessons {
		db.Model(&models.Lesson{}).Where("slug = ?", slug).Update("content", content)
	}
}
