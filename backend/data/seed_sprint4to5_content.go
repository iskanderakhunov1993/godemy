package data

import (
	"golanger/backend/models"
	"gorm.io/gorm"
)

func SeedSprint4to5Content(db *gorm.DB) {
	lessons := map[string]string{
"4-1-project-requirements": `# Разбор задачи: игра «Угадай число»

В этом спринте мы готовимся к первому настоящему проекту — консольной игре **«Угадайка»**. Прежде чем писать код, разберёмся, **что именно** должна делать программа.

## Что такое «Угадайка»

Компьютер загадывает случайное число от 1 до 100. Игрок пытается его угадать, вводя числа в консоль. После каждой попытки программа подсказывает: загаданное число **больше** или **меньше** введённого. Игра продолжается, пока игрок не угадает или не исчерпает попытки.

## Режимы сложности

Перед началом раунда игрок выбирает уровень сложности:

| Уровень | Попытки | Диапазон |
|---------|---------|----------|
| Easy    | 10      | 1–50     |
| Medium  | 7       | 1–100    |
| Hard    | 5       | 1–200    |

Чем сложнее режим — тем шире диапазон и меньше попыток.

## Функциональные требования

Программа должна:

1. **Приветствовать** игрока и объяснять правила.
2. **Предлагать выбор** уровня сложности.
3. **Генерировать** случайное число в заданном диапазоне.
4. **Принимать** ввод от пользователя (число).
5. **Сообщать**, больше или меньше загаданное число.
6. **Считать попытки** и завершать раунд при победе или проигрыше.
7. **Спрашивать**, хочет ли игрок сыграть ещё раз.
8. **Обрабатывать некорректный ввод** — если пользователь ввёл не число, программа не должна падать.

## Пример работы программы

` + "```" + `
=== Добро пожаловать в Угадайку! ===

Выберите сложность:
1 - Easy   (10 попыток, число от 1 до 50)
2 - Medium (7 попыток, число от 1 до 100)
3 - Hard   (5 попыток, число от 1 до 200)

Ваш выбор: 2

Я загадал число от 1 до 100. У вас 7 попыток.

Попытка 1/7. Введите число: 50
Моё число больше!

Попытка 2/7. Введите число: 75
Моё число меньше!

Попытка 3/7. Введите число: 63
🎉 Верно! Вы угадали за 3 попытки!

Играть ещё? (yes/no): no
Спасибо за игру!
` + "```" + `

## Какие знания нам понадобятся

Для реализации проекта мы изучим:

- **Переменные и константы** — хранить загаданное число, счётчик попыток, уровень сложности.
- **Пакет fmt** — выводить текст и форматировать строки.
- **Условия (if/else)** — сравнивать числа, проверять ввод.
- **Циклы (for)** — повторять запрос ввода, реализовать повторные раунды.
- **Генерация случайных чисел** — загадывать число.
- **Чтение ввода** — получать данные от пользователя.
- **Функции** — разбить код на логические части.

Каждой из этих тем посвящён отдельный урок в спринте. К концу спринта вы будете знать всё необходимое, чтобы написать проект самостоятельно.

## Структура проекта

Финальный проект будет иметь такую структуру:

` + "```" + `
guessing-game/
├── go.mod
├── main.go
└── README.md
` + "```" + `

Весь код поместится в один файл ` + "`main.go`" + `. Это нормально для небольшой программы. В будущих проектах мы научимся разделять код на пакеты.

## Итог

Теперь вы знаете, **что** мы будем строить. В следующих уроках мы разберём каждый кирпичик, из которого собирается игра. А в Спринте 5 — соберём всё вместе.
`,

"4-2-simple-explanation": `# Как игра работает для пользователя

Прежде чем программировать, полезно **прожить** игру глазами пользователя. В этом уроке мы пройдём полный сеанс и разберём каждый шаг.

## Сценарий: полный сеанс игры

Представьте, что вы открыли терминал и запустили программу:

` + "```bash" + `
go run main.go
` + "```" + `

На экране появляется:

` + "```" + `
=== Добро пожаловать в Угадайку! ===

Правила просты: я загадаю число, а вы попробуете его угадать.
После каждой попытки я скажу, больше или меньше моё число.

Выберите сложность:
1 - Easy   (10 попыток, число от 1 до 50)
2 - Medium (7 попыток, число от 1 до 100)
3 - Hard   (5 попыток, число от 1 до 200)

Ваш выбор:
` + "```" + `

Вы вводите ` + "`2`" + ` и нажимаете Enter:

` + "```" + `
Ваш выбор: 2

Отлично! Режим Medium.
Я загадал число от 1 до 100. У вас 7 попыток. Удачи!

Попытка 1/7. Введите число:
` + "```" + `

## Стратегия угадывания

Самый эффективный способ — **бинарный поиск**. Каждый раз делите диапазон пополам:

` + "```" + `
Попытка 1/7. Введите число: 50
Моё число больше!

Попытка 2/7. Введите число: 75
Моё число меньше!

Попытка 3/7. Введите число: 62
Моё число больше!

Попытка 4/7. Введите число: 68
Моё число меньше!

Попытка 5/7. Введите число: 65
Моё число больше!

Попытка 6/7. Введите число: 67
Моё число меньше!

Попытка 7/7. Введите число: 66
🎉 Верно! Вы угадали число 66 за 7 попыток!
` + "```" + `

При бинарном поиске 7 попыток хватает для диапазона до 128 чисел (2⁷ = 128). Значит, режим Medium (1–100) — вполне проходим!

## Что если не угадал?

` + "```" + `
Попытка 5/5. Введите число: 142
Моё число меньше!

Вы не угадали. Я загадал число 88. Попробуйте ещё раз!

Играть ещё? (yes/no):
` + "```" + `

Программа **всегда** раскрывает ответ, если игрок проиграл. Это важно для пользовательского опыта — человек не должен оставаться в неведении.

## Обработка ошибок ввода

Что если вместо числа ввести слово?

` + "```" + `
Попытка 3/7. Введите число: привет
Некорректный ввод. Пожалуйста, введите целое число.

Попытка 3/7. Введите число: 50
Моё число меньше!
` + "```" + `

Обратите внимание: ошибочный ввод **не тратит попытку**. Это справедливо — игрок ошибся в формате, а не в угадывании.

## Повторная игра

После завершения раунда:

` + "```" + `
Играть ещё? (yes/no): yes

Выберите сложность:
1 - Easy   (10 попыток, число от 1 до 50)
2 - Medium (7 попыток, число от 1 до 100)
3 - Hard   (5 попыток, число от 1 до 200)

Ваш выбор:
` + "```" + `

Если ввести ` + "`no`" + `:

` + "```" + `
Играть ещё? (yes/no): no
Спасибо за игру! До встречи!
` + "```" + `

## Разбор сценария по шагам

Давайте выделим **этапы**, которые программа проходит:

1. **Приветствие** — показать название и правила.
2. **Выбор сложности** — прочитать ввод, определить параметры.
3. **Генерация числа** — загадать случайное число в диапазоне.
4. **Цикл попыток**:
   - Показать номер попытки.
   - Прочитать ввод.
   - Проверить: корректное ли число?
   - Сравнить с загаданным.
   - Вывести подсказку или поздравление.
5. **Результат** — победа или поражение.
6. **Повтор** — спросить «Ещё раз?» и вернуться к шагу 2 или завершить.

Эти шаги — план нашей программы. Каждый шаг соответствует конструкции языка Go, которую мы изучим в следующих уроках.

## Итог

Теперь вы точно представляете, как выглядит игра с точки зрения пользователя. В следующем уроке начнём изучать конструкции Go, из которых соберём эту программу. Первая тема — **переменные и константы**.
`,

"4-3-variables-constants": `# Переменные и константы в Go

Переменные — основа любой программы. В нашей игре «Угадайка» они будут хранить загаданное число, ввод игрока, количество попыток и многое другое. Разберёмся, как создавать и использовать переменные в Go.

## Объявление через var

Ключевое слово ` + "`var`" + ` объявляет переменную с указанием типа:

` + "```go" + `
var name string
var age int
var score float64
var isReady bool
` + "```" + `

Если не присвоить значение, переменная получит **нулевое значение** своего типа:

| Тип       | Нулевое значение |
|-----------|-----------------|
| int       | 0               |
| float64   | 0.0             |
| string    | "" (пустая)     |
| bool      | false           |

Можно сразу задать значение:

` + "```go" + `
var maxAttempts int = 7
var greeting string = "Добро пожаловать!"
` + "```" + `

## Краткое объявление :=

Внутри функций можно использовать короткую форму — оператор ` + "`:=`" + `. Go сам определит тип по значению:

` + "```go" + `
maxAttempts := 7           // int
playerName := "Искандер"   // string
pi := 3.14                 // float64
isPlaying := true          // bool
` + "```" + `

Это самый частый способ объявления переменных в Go. Используйте ` + "`:=`" + ` когда значение известно сразу.

## Разница между var и :=

` + "```go" + `
package main

import "fmt"

// var работает на уровне пакета
var version = "1.0.0"

func main() {
    // := работает только внутри функций
    attempts := 0
    fmt.Println("Версия:", version)
    fmt.Println("Попытки:", attempts)
}
` + "```" + `

Оператор ` + "`:=`" + ` нельзя использовать вне функции — это ограничение языка. За пределами функций используйте ` + "`var`" + `.

## Присваивание

После объявления меняйте значение через ` + "`=`" + ` (без двоеточия):

` + "```go" + `
attempts := 0
fmt.Println(attempts) // 0

attempts = 1
fmt.Println(attempts) // 1

attempts = attempts + 1
fmt.Println(attempts) // 2
` + "```" + `

Для увеличения и уменьшения на единицу есть краткие формы:

` + "```go" + `
attempts++  // то же что attempts = attempts + 1
attempts--  // то же что attempts = attempts - 1
` + "```" + `

## Основные типы данных

Для нашего проекта нужны четыре типа:

### int — целые числа

` + "```go" + `
secretNumber := 42
maxRange := 100
attempts := 0
` + "```" + `

### float64 — числа с дробной частью

` + "```go" + `
average := 4.5
percentage := 98.7
` + "```" + `

### string — строки

` + "```go" + `
message := "Моё число больше!"
difficulty := "medium"
` + "```" + `

Строки в Go заключаются в **двойные кавычки**. Одинарные кавычки используются для отдельных символов (тип ` + "`rune`" + `).

### bool — логический тип

` + "```go" + `
isGuessed := false
wantsToPlay := true
` + "```" + `

Может принимать только два значения: ` + "`true`" + ` или ` + "`false`" + `.

## Константы

Константы — это значения, которые **нельзя изменить** после объявления. Объявляются через ` + "`const`" + `:

` + "```go" + `
const maxDifficulty = 3
const gameName = "Угадайка"
` + "```" + `

Попытка изменить константу вызовет ошибку компиляции:

` + "```go" + `
const maxDifficulty = 3
maxDifficulty = 5 // ошибка: cannot assign to maxDifficulty
` + "```" + `

Можно объявить несколько констант в блоке:

` + "```go" + `
const (
    easyAttempts   = 10
    mediumAttempts = 7
    hardAttempts   = 5

    easyMax   = 50
    mediumMax = 100
    hardMax   = 200
)
` + "```" + `

Используйте константы для значений, которые не должны меняться во время работы программы — границы диапазонов, количество попыток по уровням сложности.

## Пример: переменные для игры

Вот как выглядят переменные, которые понадобятся в «Угадайке»:

` + "```go" + `
package main

import "fmt"

const (
    easyAttempts   = 10
    mediumAttempts = 7
    hardAttempts   = 5
)

func main() {
    // Параметры раунда
    maxAttempts := mediumAttempts
    maxNumber := 100
    secretNumber := 42 // пока без рандома

    // Состояние игры
    attempts := 0
    isGuessed := false
    var guess int

    fmt.Println("Загадано число от 1 до", maxNumber)
    fmt.Println("У вас", maxAttempts, "попыток")
    fmt.Println("Секрет:", secretNumber)
    fmt.Println("Попыток использовано:", attempts)
    fmt.Println("Угадано:", isGuessed)
    fmt.Println("Текущий ввод:", guess) // 0 (нулевое значение)
}
` + "```" + `

## Множественное объявление

Можно объявить несколько переменных в одной строке:

` + "```go" + `
var a, b, c int           // три переменные типа int
x, y := 10, 20            // короткое объявление двух переменных
name, age := "Go", 15     // разные типы тоже работают
` + "```" + `

## Итог

- ` + "`var`" + ` — объявление с явным типом, работает везде.
- ` + "`:=`" + ` — короткое объявление, только внутри функций.
- ` + "`const`" + ` — неизменяемое значение.
- Основные типы: ` + "`int`" + `, ` + "`float64`" + `, ` + "`string`" + `, ` + "`bool`" + `.
- Нулевые значения: ` + "`0`" + `, ` + "`0.0`" + `, ` + "`\"\"`" + `, ` + "`false`" + `.

В следующем уроке научимся **выводить** эти переменные красиво — с помощью пакета ` + "`fmt`" + `.
`,

"4-4-fmt-package": `# Пакет fmt: вывод текста в консоль

Пакет ` + "`fmt`" + ` — один из самых используемых в Go. Он отвечает за форматированный ввод-вывод. В нашей игре мы будем постоянно выводить подсказки, сообщения и результаты. Разберём основные функции.

## fmt.Println

Выводит значения через пробел и добавляет перенос строки в конце:

` + "```go" + `
package main

import "fmt"

func main() {
    fmt.Println("Привет, мир!")
    fmt.Println("Попытка", 1, "из", 7)
    fmt.Println("Результат:", true)
}
` + "```" + `

Вывод:

` + "```" + `
Привет, мир!
Попытка 1 из 7
Результат: true
` + "```" + `

` + "`Println`" + ` автоматически вставляет пробелы между аргументами и ` + "`\\n`" + ` в конце. Это самый простой способ вывести что-то в консоль.

## fmt.Print

Как ` + "`Println`" + `, но **без** переноса строки:

` + "```go" + `
fmt.Print("Введите число: ")
// курсор остаётся на той же строке — удобно для ввода
` + "```" + `

Несколько вызовов ` + "`Print`" + ` выведут текст в одну строку:

` + "```go" + `
fmt.Print("Один ")
fmt.Print("Два ")
fmt.Print("Три")
fmt.Println() // перенос строки
` + "```" + `

Вывод: ` + "`Один Два Три`" + `

## fmt.Printf — форматированный вывод

Самая мощная функция. Первый аргумент — **шаблон** с плейсхолдерами, остальные — значения для подстановки:

` + "```go" + `
name := "Искандер"
score := 42

fmt.Printf("Игрок: %s, очки: %d\n", name, score)
` + "```" + `

Вывод: ` + "`Игрок: Искандер, очки: 42`" + `

Обратите внимание: ` + "`Printf`" + ` **не добавляет** перенос строки автоматически. Нужно писать ` + "`\\n`" + ` вручную.

## Основные плейсхолдеры (глаголы форматирования)

| Плейсхолдер | Назначение            | Пример                            |
|-------------|----------------------|-----------------------------------|
| %d          | Целое число          | ` + "`fmt.Printf(\"%d\", 42)`" + ` → 42     |
| %s          | Строка               | ` + "`fmt.Printf(\"%s\", \"Go\")`" + ` → Go   |
| %f          | Дробное число        | ` + "`fmt.Printf(\"%f\", 3.14)`" + ` → 3.140000 |
| %v          | Любое значение       | ` + "`fmt.Printf(\"%v\", true)`" + ` → true  |
| %t          | Логическое значение  | ` + "`fmt.Printf(\"%t\", false)`" + ` → false |
| %%          | Символ процента      | ` + "`fmt.Printf(\"100%%\")`" + ` → 100%     |

## Практические примеры

### Вывод попытки в игре

` + "```go" + `
attempt := 3
maxAttempts := 7

fmt.Printf("Попытка %d/%d. Введите число: ", attempt, maxAttempts)
` + "```" + `

Вывод: ` + "`Попытка 3/7. Введите число: `" + ` (без переноса — ждём ввод)

### Подсказка

` + "```go" + `
guess := 50
secret := 73

if guess < secret {
    fmt.Printf("Моё число больше, чем %d!\n", guess)
} else {
    fmt.Printf("Моё число меньше, чем %d!\n", guess)
}
` + "```" + `

### Результат раунда

` + "```go" + `
secret := 66
attempts := 4

fmt.Printf("🎉 Вы угадали число %d за %d попытки!\n", secret, attempts)
` + "```" + `

### Форматирование дробных чисел

` + "```go" + `
percentage := 85.7142857

fmt.Printf("Точность: %f\n", percentage)    // 85.714286 (6 знаков по умолчанию)
fmt.Printf("Точность: %.2f\n", percentage)  // 85.71 (2 знака)
fmt.Printf("Точность: %.0f%%\n", percentage) // 86% (округление, без дробной части)
` + "```" + `

## Sprintf — форматирование в строку

Иногда нужно не вывести текст, а **сохранить** его в переменную. Для этого есть ` + "`fmt.Sprintf`" + `:

` + "```go" + `
attempt := 3
maxAttempts := 7

prompt := fmt.Sprintf("Попытка %d/%d. Введите число: ", attempt, maxAttempts)
fmt.Print(prompt)
` + "```" + `

` + "`Sprintf`" + ` возвращает строку, а не выводит её. Это полезно, когда нужно сформировать сообщение заранее.

## Универсальный %v

Если не хотите думать о типе, используйте ` + "`%v`" + ` — он подставит значение в «стандартном» виде:

` + "```go" + `
fmt.Printf("Число: %v\n", 42)       // Число: 42
fmt.Printf("Строка: %v\n", "Go")    // Строка: Go
fmt.Printf("Логика: %v\n", true)    // Логика: true
fmt.Printf("Дробь: %v\n", 3.14)    // Дробь: 3.14
` + "```" + `

Для отладки ` + "`%v`" + ` незаменим. Но в финальном коде лучше использовать конкретные плейсхолдеры — код читается яснее.

## Полный пример: начало игры

` + "```go" + `
package main

import "fmt"

func main() {
    gameName := "Угадайка"
    version := "1.0"

    fmt.Println("===", gameName, "===")
    fmt.Printf("Версия: %s\n\n", version)

    fmt.Println("Выберите сложность:")
    fmt.Printf("  %d - %-8s (%d попыток, число от 1 до %d)\n", 1, "Easy", 10, 50)
    fmt.Printf("  %d - %-8s (%d попыток, число от 1 до %d)\n", 2, "Medium", 7, 100)
    fmt.Printf("  %d - %-8s (%d попыток, число от 1 до %d)\n", 3, "Hard", 5, 200)

    fmt.Print("\nВаш выбор: ")
}
` + "```" + `

Здесь ` + "`%-8s`" + ` — выравнивание строки влево в поле шириной 8 символов. Это помогает выровнять столбцы.

## Итог

- ` + "`fmt.Println`" + ` — вывод с переносом строки.
- ` + "`fmt.Print`" + ` — вывод без переноса.
- ` + "`fmt.Printf`" + ` — форматированный вывод с плейсхолдерами.
- ` + "`fmt.Sprintf`" + ` — форматирование в строку.
- Основные плейсхолдеры: ` + "`%d`" + `, ` + "`%s`" + `, ` + "`%f`" + `, ` + "`%v`" + `.

В следующем уроке изучим **условия** — конструкцию ` + "`if/else`" + `, без которой невозможно сравнить числа.
`,

"4-5-conditions": `# Условия: if, else if, else

Условия позволяют программе принимать решения. В «Угадайке» они нужны повсюду: сравнить числа, проверить ввод, определить победу или поражение. Разберём конструкцию ` + "`if`" + ` в Go.

## Базовый if

` + "```go" + `
age := 18

if age >= 18 {
    fmt.Println("Доступ разрешён")
}
` + "```" + `

Фигурные скобки ` + "`{}`" + ` **обязательны** в Go, даже для одной строки. Круглых скобок вокруг условия не нужно (в отличие от C или Java).

## if-else

` + "```go" + `
guess := 50
secret := 73

if guess == secret {
    fmt.Println("Угадали!")
} else {
    fmt.Println("Не угадали!")
}
` + "```" + `

## if — else if — else

` + "```go" + `
guess := 50
secret := 73

if guess == secret {
    fmt.Println("🎉 Верно!")
} else if guess < secret {
    fmt.Println("Моё число больше!")
} else {
    fmt.Println("Моё число меньше!")
}
` + "```" + `

Это ключевая логика нашей игры. Три ветки: угадал, загаданное больше, загаданное меньше.

## Операторы сравнения

| Оператор | Значение          | Пример         |
|----------|-------------------|----------------|
| ==       | Равно             | ` + "`a == b`" + `     |
| !=       | Не равно          | ` + "`a != b`" + `     |
| <        | Меньше            | ` + "`a < b`" + `      |
| >        | Больше            | ` + "`a > b`" + `      |
| <=       | Меньше или равно  | ` + "`a <= b`" + `     |
| >=       | Больше или равно  | ` + "`a >= b`" + `     |

## Логические операторы

Можно комбинировать условия:

` + "```go" + `
age := 25
hasTicket := true

// И (оба условия должны быть true)
if age >= 18 && hasTicket {
    fmt.Println("Проходите")
}

// ИЛИ (хотя бы одно true)
if age < 12 || age > 65 {
    fmt.Println("Скидка")
}

// НЕ (инвертирует)
if !hasTicket {
    fmt.Println("Купите билет")
}
` + "```" + `

## if с инициализацией

В Go есть уникальная конструкция — можно объявить переменную прямо в ` + "`if`" + `:

` + "```go" + `
if attempts := 7; attempts > 5 {
    fmt.Printf("Осталось много попыток: %d\n", attempts)
}
// attempts не существует за пределами if
` + "```" + `

Переменная ` + "`attempts`" + ` существует **только** внутри блока ` + "`if`" + `/` + "`else`" + `. Это удобно, когда переменная нужна только для проверки.

Часто этот приём используют с функциями, возвращающими ошибку:

` + "```go" + `
if err := doSomething(); err != nil {
    fmt.Println("Ошибка:", err)
}
` + "```" + `

Мы подробнее разберём этот паттерн в уроке про чтение ввода.

## Примеры для «Угадайки»

### Выбор сложности

` + "```go" + `
choice := 2

var maxAttempts int
var maxNumber int

if choice == 1 {
    maxAttempts = 10
    maxNumber = 50
    fmt.Println("Режим: Easy")
} else if choice == 2 {
    maxAttempts = 7
    maxNumber = 100
    fmt.Println("Режим: Medium")
} else if choice == 3 {
    maxAttempts = 5
    maxNumber = 200
    fmt.Println("Режим: Hard")
} else {
    fmt.Println("Неизвестный режим. Выбран Medium.")
    maxAttempts = 7
    maxNumber = 100
}

fmt.Printf("Попыток: %d, диапазон: 1–%d\n", maxAttempts, maxNumber)
` + "```" + `

### Проверка ввода

` + "```go" + `
guess := -5
maxNumber := 100

if guess < 1 || guess > maxNumber {
    fmt.Printf("Введите число от 1 до %d\n", maxNumber)
} else {
    fmt.Printf("Вы ввели: %d\n", guess)
}
` + "```" + `

### Конец раунда

` + "```go" + `
isGuessed := false
attempts := 7
maxAttempts := 7
secret := 42

if isGuessed {
    fmt.Printf("Вы угадали за %d попыток!\n", attempts)
} else if attempts >= maxAttempts {
    fmt.Printf("Попытки закончились. Было загадано: %d\n", secret)
}
` + "```" + `

## Вложенные условия

Условия можно вкладывать друг в друга, но не злоупотребляйте — глубокая вложенность ухудшает читаемость:

` + "```go" + `
guess := 50
secret := 73
attempts := 3
maxAttempts := 7

if guess == secret {
    fmt.Println("Угадали!")
} else {
    if guess < secret {
        fmt.Println("Моё число больше!")
    } else {
        fmt.Println("Моё число меньше!")
    }

    if attempts >= maxAttempts {
        fmt.Printf("Попытки кончились! Ответ: %d\n", secret)
    }
}
` + "```" + `

Лучше переписать плоско с ` + "`else if`" + ` или ранними возвратами (об этом — в уроке про функции).

## Полный пример

` + "```go" + `
package main

import "fmt"

func main() {
    secret := 73
    guess := 50
    attempt := 3
    maxAttempts := 7

    fmt.Printf("Попытка %d/%d.\n", attempt, maxAttempts)

    if guess == secret {
        fmt.Printf("🎉 Вы угадали число %d!\n", secret)
    } else if guess < secret {
        fmt.Printf("%d — мало. Моё число больше!\n", guess)
        remaining := maxAttempts - attempt
        if remaining > 0 {
            fmt.Printf("Осталось попыток: %d\n", remaining)
        } else {
            fmt.Printf("Это была последняя попытка. Ответ: %d\n", secret)
        }
    } else {
        fmt.Printf("%d — много. Моё число меньше!\n", guess)
        remaining := maxAttempts - attempt
        if remaining > 0 {
            fmt.Printf("Осталось попыток: %d\n", remaining)
        } else {
            fmt.Printf("Это была последняя попытка. Ответ: %d\n", secret)
        }
    }
}
` + "```" + `

## Итог

- ` + "`if`" + ` / ` + "`else if`" + ` / ` + "`else`" + ` — основная конструкция ветвления.
- Скобки ` + "`{}`" + ` обязательны, круглые ` + "`()`" + ` не нужны.
- Операторы сравнения: ` + "`==`" + `, ` + "`!=`" + `, ` + "`<`" + `, ` + "`>`" + `, ` + "`<=`" + `, ` + "`>=`" + `.
- Логические операторы: ` + "`&&`" + `, ` + "`||`" + `, ` + "`!`" + `.
- ` + "`if`" + ` с инициализацией — удобный паттерн Go.

В следующем уроке — **циклы**, которые позволят повторять попытки ввода.
`,

"4-6-loops": `# Циклы в Go: for

В Go есть только одно ключевое слово для циклов — ` + "`for`" + `. Но оно покрывает все случаи: цикл со счётчиком, цикл с условием (аналог ` + "`while`" + `) и бесконечный цикл. В «Угадайке» циклы нужны для повторных попыток ввода и для повторных раундов.

## Цикл со счётчиком

Классический цикл с начальным значением, условием и шагом:

` + "```go" + `
for i := 1; i <= 5; i++ {
    fmt.Printf("Попытка %d\n", i)
}
` + "```" + `

Вывод:

` + "```" + `
Попытка 1
Попытка 2
Попытка 3
Попытка 4
Попытка 5
` + "```" + `

Три части разделены точками с запятой:
1. **Инициализация**: ` + "`i := 1`" + ` — выполняется один раз перед циклом.
2. **Условие**: ` + "`i <= 5`" + ` — проверяется перед каждой итерацией.
3. **Шаг**: ` + "`i++`" + ` — выполняется после каждой итерации.

## Цикл с условием (while-стиль)

Если убрать инициализацию и шаг, получится аналог ` + "`while`" + ` из других языков:

` + "```go" + `
attempts := 0
maxAttempts := 7

for attempts < maxAttempts {
    attempts++
    fmt.Printf("Попытка %d/%d\n", attempts, maxAttempts)
}
` + "```" + `

Цикл выполняется, **пока условие истинно**.

## Бесконечный цикл

` + "`for`" + ` без условия — бесконечный цикл:

` + "```go" + `
for {
    fmt.Println("Это будет повторяться вечно")
}
` + "```" + `

В реальности бесконечные циклы используют с ` + "`break`" + ` — выходом по условию изнутри. Именно так мы сделаем цикл ввода в игре.

## break — выход из цикла

` + "```go" + `
for i := 1; i <= 100; i++ {
    if i == 5 {
        fmt.Println("Нашли 5, выходим!")
        break
    }
    fmt.Println(i)
}
` + "```" + `

Вывод:

` + "```" + `
1
2
3
4
Нашли 5, выходим!
` + "```" + `

` + "`break`" + ` мгновенно прерывает ближайший цикл.

## continue — пропуск итерации

` + "```go" + `
for i := 1; i <= 10; i++ {
    if i%2 == 0 {
        continue // пропускаем чётные
    }
    fmt.Println(i) // печатаем только нечётные
}
` + "```" + `

Вывод: ` + "`1 3 5 7 9`" + ` (каждое на новой строке)

` + "`continue`" + ` переходит к следующей итерации, пропуская оставшийся код в теле цикла.

## Примеры для «Угадайки»

### Цикл попыток

` + "```go" + `
secret := 42
maxAttempts := 7

for attempt := 1; attempt <= maxAttempts; attempt++ {
    fmt.Printf("Попытка %d/%d. Введите число: ", attempt, maxAttempts)

    guess := 50 // пока подставляем фиксированное значение

    if guess == secret {
        fmt.Println("Угадали!")
        break
    } else if guess < secret {
        fmt.Println("Моё число больше!")
    } else {
        fmt.Println("Моё число меньше!")
    }
}
` + "```" + `

Здесь ` + "`break`" + ` — ключевой момент. Если игрок угадал, выходим из цикла досрочно.

### Бесконечный цикл с break — повторная игра

` + "```go" + `
for {
    fmt.Println("--- Новый раунд ---")
    // ... логика раунда ...

    fmt.Print("Играть ещё? (yes/no): ")
    answer := "no" // пока подставляем

    if answer != "yes" {
        fmt.Println("Спасибо за игру!")
        break
    }
}
` + "```" + `

### Цикл с валидацией ввода

` + "```go" + `
var choice int

for {
    fmt.Print("Выберите сложность (1-3): ")
    choice = 2 // пока подставляем

    if choice >= 1 && choice <= 3 {
        break // корректный ввод, выходим
    }
    fmt.Println("Неверный выбор. Попробуйте снова.")
}

fmt.Printf("Вы выбрали: %d\n", choice)
` + "```" + `

Этот паттерн — **бесконечный цикл + проверка + break** — очень частый в Go. Запомните его.

## Вложенные циклы

Циклы можно вкладывать. В нашей игре это будет:
- **Внешний цикл** — повторные раунды («Играть ещё?»).
- **Внутренний цикл** — попытки угадать число.

` + "```go" + `
// Внешний цикл — раунды
for {
    secret := 42
    maxAttempts := 7
    isGuessed := false

    // Внутренний цикл — попытки
    for attempt := 1; attempt <= maxAttempts; attempt++ {
        guess := 50 // пока подставляем

        if guess == secret {
            fmt.Printf("Угадали за %d попыток!\n", attempt)
            isGuessed = true
            break
        }
    }

    if !isGuessed {
        fmt.Printf("Не угадали. Ответ: %d\n", secret)
    }

    fmt.Print("Ещё раз? (yes/no): ")
    answer := "no"
    if answer != "yes" {
        break
    }
}
` + "```" + `

## Полный пример: обратный отсчёт

` + "```go" + `
package main

import "fmt"

func main() {
    fmt.Println("=== Демонстрация циклов ===\n")

    // Цикл со счётчиком
    fmt.Println("Обратный отсчёт:")
    for i := 5; i >= 1; i-- {
        fmt.Printf("%d... ", i)
    }
    fmt.Println("Пуск!")

    // While-стиль
    fmt.Println("\nУдвоение:")
    n := 1
    for n < 100 {
        fmt.Println(n)
        n *= 2
    }

    // Бесконечный с break
    fmt.Println("\nПоиск числа:")
    x := 0
    for {
        x += 3
        if x > 20 {
            fmt.Printf("Первое кратное 3 больше 20: %d\n", x)
            break
        }
    }
}
` + "```" + `

## Итог

- ` + "`for i := 0; i < n; i++`" + ` — цикл со счётчиком.
- ` + "`for condition`" + ` — цикл с условием (аналог while).
- ` + "`for`" + ` — бесконечный цикл.
- ` + "`break`" + ` — выход из цикла.
- ` + "`continue`" + ` — пропуск итерации.

В следующем уроке научимся **генерировать случайные числа** — без этого загадать число не получится.
`,

"4-7-random": `# Генерация случайных чисел

Сердце «Угадайки» — случайное число, которое загадывает компьютер. В Go для этого используется пакет ` + "`math/rand`" + `. Разберём, как он работает.

## Базовая генерация

Начиная с Go 1.20, генератор случайных чисел автоматически инициализируется случайным seed-значением. Вам не нужно вызывать ` + "`rand.Seed()`" + ` — это устаревший подход.

` + "```go" + `
package main

import (
    "fmt"
    "math/rand"
)

func main() {
    // Случайное число от 0 до 99
    n := rand.Intn(100)
    fmt.Println(n)
}
` + "```" + `

Каждый запуск программы будет давать **разные** числа.

## rand.Intn(n)

Функция ` + "`rand.Intn(n)`" + ` возвращает случайное целое число в диапазоне ` + "`[0, n)`" + `, то есть от 0 до n-1 **включительно**:

` + "```go" + `
rand.Intn(6)   // от 0 до 5
rand.Intn(100) // от 0 до 99
rand.Intn(1)   // всегда 0
` + "```" + `

## Число в произвольном диапазоне

Чтобы получить число от ` + "`min`" + ` до ` + "`max`" + ` включительно, используйте формулу:

` + "```go" + `
// число от min до max включительно
number := rand.Intn(max-min+1) + min
` + "```" + `

Разберём на примере — число от 1 до 100:

` + "```go" + `
secret := rand.Intn(100) + 1
// rand.Intn(100) даёт 0..99
// +1 сдвигает в 1..100
` + "```" + `

Для разных уровней сложности:

` + "```go" + `
package main

import (
    "fmt"
    "math/rand"
)

func main() {
    // Easy: 1–50
    easyNumber := rand.Intn(50) + 1
    fmt.Println("Easy:", easyNumber)

    // Medium: 1–100
    mediumNumber := rand.Intn(100) + 1
    fmt.Println("Medium:", mediumNumber)

    // Hard: 1–200
    hardNumber := rand.Intn(200) + 1
    fmt.Println("Hard:", hardNumber)
}
` + "```" + `

## Что такое seed (зерно)

Генератор случайных чисел на самом деле **псевдослучайный** — он вычисляет числа по формуле, начиная с начального значения (seed). Одинаковый seed всегда даёт одинаковую последовательность.

В Go версий до 1.20 нужно было вручную задавать seed:

` + "```go" + `
// Старый подход (Go < 1.20) — НЕ НУЖЕН в новых версиях
// rand.Seed(time.Now().UnixNano())
` + "```" + `

Начиная с Go 1.20 генератор автоматически получает случайный seed при запуске программы. Но если вам нужна **воспроизводимая** последовательность (например, для тестов), можно создать генератор с фиксированным seed:

` + "```go" + `
// Генератор с фиксированным seed — всегда одна и та же последовательность
r := rand.New(rand.NewSource(42))
fmt.Println(r.Intn(100)) // всегда одно и то же число
fmt.Println(r.Intn(100)) // и следующее тоже предсказуемо
` + "```" + `

Для игры фиксированный seed не нужен — мы хотим, чтобы числа были разными каждый раз.

## Другие функции rand

` + "```go" + `
// Случайное число типа float64 от 0.0 до 1.0
f := rand.Float64()
fmt.Printf("Случайная дробь: %.4f\n", f)

// Случайное число типа int (может быть очень большим)
big := rand.Int()
fmt.Println("Большое число:", big)
` + "```" + `

Для «Угадайки» нам достаточно ` + "`rand.Intn()`" + `.

## Пример: функция генерации для игры

` + "```go" + `
package main

import (
    "fmt"
    "math/rand"
)

func generateSecret(maxNumber int) int {
    return rand.Intn(maxNumber) + 1
}

func main() {
    // Имитация выбора сложности
    difficulty := 2 // Medium
    var maxNumber int

    if difficulty == 1 {
        maxNumber = 50
    } else if difficulty == 2 {
        maxNumber = 100
    } else {
        maxNumber = 200
    }

    secret := generateSecret(maxNumber)
    fmt.Printf("Загадано число от 1 до %d\n", maxNumber)
    fmt.Printf("(Секрет: %d)\n", secret) // Для отладки!
}
` + "```" + `

В финальной версии строку с секретом нужно **убрать**, иначе играть будет неинтересно.

## Проверка распределения

Хотите убедиться, что числа действительно случайные? Сгенерируйте много и посмотрите:

` + "```go" + `
package main

import (
    "fmt"
    "math/rand"
)

func main() {
    counts := make(map[int]int)

    for i := 0; i < 10000; i++ {
        n := rand.Intn(10) + 1 // 1–10
        counts[n]++
    }

    fmt.Println("Распределение (10 000 бросков, 1–10):")
    for i := 1; i <= 10; i++ {
        fmt.Printf("  %2d: %d раз\n", i, counts[i])
    }
}
` + "```" + `

Каждое число выпадет примерно 1000 раз — распределение равномерное.

## Итог

- ` + "`math/rand`" + ` — пакет для генерации псевдослучайных чисел.
- ` + "`rand.Intn(n)`" + ` — число от 0 до n-1.
- ` + "`rand.Intn(max) + 1`" + ` — число от 1 до max.
- В Go 1.20+ seed задаётся автоматически.
- Для тестов можно создать генератор с фиксированным seed.

В следующем уроке научимся **читать ввод** пользователя из консоли.
`,

"4-8-input": `# Чтение ввода пользователя

Игра без ввода — не игра. В «Угадайке» пользователь вводит уровень сложности, числа-догадки и ответ «играть ещё?». Разберём способы чтения ввода в Go.

## fmt.Scan — простое чтение

Самый простой способ — функция ` + "`fmt.Scan`" + `:

` + "```go" + `
package main

import "fmt"

func main() {
    var guess int

    fmt.Print("Введите число: ")
    fmt.Scan(&guess)

    fmt.Printf("Вы ввели: %d\n", guess)
}
` + "```" + `

Обратите внимание на ` + "`&`" + ` перед переменной. Это **оператор взятия адреса** — ` + "`fmt.Scan`" + ` получает указатель на переменную, чтобы записать в неё значение. Пока не углубляемся в указатели — просто запомните: для ` + "`Scan`" + ` нужен ` + "`&`" + `.

` + "`fmt.Scan`" + ` читает до первого пробела или переноса строки. Для чисел этого достаточно.

## Обработка ошибок fmt.Scan

` + "`fmt.Scan`" + ` возвращает количество успешно прочитанных значений и ошибку:

` + "```go" + `
var guess int

fmt.Print("Введите число: ")
n, err := fmt.Scan(&guess)

if err != nil {
    fmt.Println("Ошибка ввода:", err)
} else {
    fmt.Printf("Прочитано %d значений. Число: %d\n", n, guess)
}
` + "```" + `

Если пользователь введёт ` + "`привет`" + ` вместо числа, ` + "`err`" + ` будет не ` + "`nil`" + `, а ` + "`guess`" + ` останется 0.

## bufio.Scanner — чтение строк

Для чтения строк (например, ` + "`yes`" + `/` + "`no`" + `) удобнее использовать ` + "`bufio.Scanner`" + `:

` + "```go" + `
package main

import (
    "bufio"
    "fmt"
    "os"
)

func main() {
    scanner := bufio.NewScanner(os.Stdin)

    fmt.Print("Как вас зовут? ")
    scanner.Scan()          // читаем строку
    name := scanner.Text()  // получаем текст

    fmt.Printf("Привет, %s!\n", name)
}
` + "```" + `

` + "`bufio.NewScanner(os.Stdin)`" + ` создаёт сканер, привязанный к стандартному вводу. Метод ` + "`Scan()`" + ` читает одну строку, ` + "`Text()`" + ` возвращает её содержимое.

## strconv.Atoi — строка в число

Если читаем строку сканером, а нужно число, используем ` + "`strconv.Atoi`" + ` (Ascii TO Integer):

` + "```go" + `
package main

import (
    "bufio"
    "fmt"
    "os"
    "strconv"
)

func main() {
    scanner := bufio.NewScanner(os.Stdin)

    fmt.Print("Введите число: ")
    scanner.Scan()
    input := scanner.Text()

    number, err := strconv.Atoi(input)
    if err != nil {
        fmt.Printf("'%s' — это не число!\n", input)
        return
    }

    fmt.Printf("Вы ввели: %d\n", number)
}
` + "```" + `

` + "`strconv.Atoi`" + ` возвращает два значения: число и ошибку. Если строку нельзя преобразовать, ошибка будет не ` + "`nil`" + `.

## Паттерн: цикл с валидацией ввода

В реальных программах нужно **повторять** запрос, пока ввод некорректен:

` + "```go" + `
package main

import (
    "bufio"
    "fmt"
    "os"
    "strconv"
)

func main() {
    scanner := bufio.NewScanner(os.Stdin)
    var guess int

    for {
        fmt.Print("Введите число от 1 до 100: ")
        scanner.Scan()
        input := scanner.Text()

        number, err := strconv.Atoi(input)
        if err != nil {
            fmt.Printf("'%s' — не число. Попробуйте снова.\n", input)
            continue
        }

        if number < 1 || number > 100 {
            fmt.Printf("%d вне диапазона 1–100. Попробуйте снова.\n", number)
            continue
        }

        guess = number
        break
    }

    fmt.Printf("Принято: %d\n", guess)
}
` + "```" + `

Этот паттерн — **бесконечный цикл + continue при ошибке + break при успехе** — основа обработки ввода в нашей игре.

## Пример: чтение строки yes/no

` + "```go" + `
scanner := bufio.NewScanner(os.Stdin)

for {
    fmt.Print("Играть ещё? (yes/no): ")
    scanner.Scan()
    answer := scanner.Text()

    if answer == "yes" {
        fmt.Println("Начинаем новый раунд!")
        break
    } else if answer == "no" {
        fmt.Println("Спасибо за игру!")
        break
    } else {
        fmt.Println("Введите 'yes' или 'no'.")
    }
}
` + "```" + `

## fmt.Scan vs bufio.Scanner

| Критерий             | fmt.Scan              | bufio.Scanner          |
|---------------------|-----------------------|------------------------|
| Чтение числа        | Удобно (автопарсинг)  | Нужен strconv.Atoi     |
| Чтение строки       | До первого пробела    | Вся строка             |
| Обработка ошибок    | Через возврат ошибки  | Через strconv          |
| Буферизация         | Нет                   | Да                     |

Для нашей игры мы будем использовать ` + "`bufio.Scanner`" + ` + ` + "`strconv.Atoi`" + ` — это даёт полный контроль над вводом и обработкой ошибок.

## Полный пример: выбор сложности

` + "```go" + `
package main

import (
    "bufio"
    "fmt"
    "os"
    "strconv"
)

func main() {
    scanner := bufio.NewScanner(os.Stdin)

    fmt.Println("Выберите сложность:")
    fmt.Println("1 - Easy   (10 попыток)")
    fmt.Println("2 - Medium (7 попыток)")
    fmt.Println("3 - Hard   (5 попыток)")

    var choice int
    for {
        fmt.Print("\nВаш выбор: ")
        scanner.Scan()
        input := scanner.Text()

        number, err := strconv.Atoi(input)
        if err != nil {
            fmt.Printf("'%s' — введите число от 1 до 3.\n", input)
            continue
        }

        if number < 1 || number > 3 {
            fmt.Printf("%d — нет такого уровня. Введите 1, 2 или 3.\n", number)
            continue
        }

        choice = number
        break
    }

    var maxAttempts int
    if choice == 1 {
        maxAttempts = 10
    } else if choice == 2 {
        maxAttempts = 7
    } else {
        maxAttempts = 5
    }

    fmt.Printf("Отлично! У вас %d попыток. Удачи!\n", maxAttempts)
}
` + "```" + `

## Итог

- ` + "`fmt.Scan(&var)`" + ` — быстрое чтение числа.
- ` + "`bufio.NewScanner(os.Stdin)`" + ` — чтение строк целиком.
- ` + "`strconv.Atoi(s)`" + ` — преобразование строки в число с обработкой ошибки.
- Паттерн «цикл + continue + break» — надёжная валидация ввода.

В следующем уроке — **функции**, чтобы разбить растущий код на логические блоки.
`,

"4-9-functions": `# Функции в Go

Когда программа растёт, держать весь код в ` + "`main()`" + ` становится неудобно. Функции позволяют разбить код на именованные блоки, каждый из которых решает одну задачу. В «Угадайке» мы вынесем генерацию числа, чтение ввода и игровой раунд в отдельные функции.

## Базовый синтаксис

` + "```go" + `
func greet() {
    fmt.Println("Добро пожаловать в Угадайку!")
}

func main() {
    greet() // вызов функции
}
` + "```" + `

Функция объявляется через ` + "`func`" + `, затем имя, круглые скобки и тело в фигурных скобках.

## Функции с параметрами

` + "```go" + `
func printAttempt(current int, max int) {
    fmt.Printf("Попытка %d/%d\n", current, max)
}

func main() {
    printAttempt(3, 7)
}
` + "```" + `

Если параметры одного типа, можно сократить:

` + "```go" + `
func printAttempt(current, max int) {
    fmt.Printf("Попытка %d/%d\n", current, max)
}
` + "```" + `

## Возвращаемое значение

` + "```go" + `
func generateSecret(maxNumber int) int {
    return rand.Intn(maxNumber) + 1
}

func main() {
    secret := generateSecret(100)
    fmt.Println("Загадано:", secret)
}
` + "```" + `

Тип возвращаемого значения указывается после скобок с параметрами. ` + "`return`" + ` завершает функцию и возвращает результат.

## Множественные возвращаемые значения

Это одна из визитных карточек Go. Функция может возвращать несколько значений:

` + "```go" + `
func getDifficulty(choice int) (int, int) {
    if choice == 1 {
        return 10, 50  // maxAttempts, maxNumber
    } else if choice == 2 {
        return 7, 100
    }
    return 5, 200
}

func main() {
    maxAttempts, maxNumber := getDifficulty(2)
    fmt.Printf("Попыток: %d, диапазон: 1–%d\n", maxAttempts, maxNumber)
}
` + "```" + `

Множественные возвращаемые значения особенно полезны для возврата **результата и ошибки**:

` + "```go" + `
func readNumber(scanner *bufio.Scanner, min, max int) (int, error) {
    scanner.Scan()
    input := scanner.Text()

    number, err := strconv.Atoi(input)
    if err != nil {
        return 0, fmt.Errorf("'%s' — не число", input)
    }

    if number < min || number > max {
        return 0, fmt.Errorf("%d вне диапазона %d–%d", number, min, max)
    }

    return number, nil
}
` + "```" + `

` + "`nil`" + ` в качестве ошибки означает «всё хорошо». Если ошибка не ` + "`nil`" + ` — что-то пошло не так.

## Зачем разбивать код на функции

Сравните два подхода:

**Без функций** — всё в ` + "`main()`" + `:

` + "```go" + `
func main() {
    // 20 строк: приветствие
    // 30 строк: выбор сложности
    // 40 строк: игровой цикл
    // 15 строк: повторная игра
    // Итого: 105 строк в одной функции
}
` + "```" + `

**С функциями** — каждая задача отдельно:

` + "```go" + `
func main() {
    greet()
    for {
        maxAttempts, maxNumber := chooseDifficulty(scanner)
        playRound(scanner, maxAttempts, maxNumber)
        if !askPlayAgain(scanner) {
            break
        }
    }
    fmt.Println("Спасибо за игру!")
}
` + "```" + `

Второй вариант читается как **план**: приветствие, выбор сложности, раунд, вопрос о повторе. Каждую функцию можно понять, написать и протестировать отдельно.

## Пример: функции для «Угадайки»

` + "```go" + `
package main

import (
    "bufio"
    "fmt"
    "math/rand"
    "os"
    "strconv"
)

func greet() {
    fmt.Println("=== Добро пожаловать в Угадайку! ===")
    fmt.Println("Я загадаю число, а вы попробуете угадать.")
    fmt.Println()
}

func chooseDifficulty(scanner *bufio.Scanner) (int, int) {
    fmt.Println("Выберите сложность:")
    fmt.Println("1 - Easy   (10 попыток, 1–50)")
    fmt.Println("2 - Medium (7 попыток, 1–100)")
    fmt.Println("3 - Hard   (5 попыток, 1–200)")

    for {
        fmt.Print("Ваш выбор: ")
        scanner.Scan()
        input := scanner.Text()

        choice, err := strconv.Atoi(input)
        if err != nil || choice < 1 || choice > 3 {
            fmt.Println("Введите 1, 2 или 3.")
            continue
        }

        if choice == 1 {
            return 10, 50
        } else if choice == 2 {
            return 7, 100
        }
        return 5, 200
    }
}

func playRound(scanner *bufio.Scanner, maxAttempts, maxNumber int) {
    secret := rand.Intn(maxNumber) + 1
    fmt.Printf("\nЯ загадал число от 1 до %d. У вас %d попыток.\n", maxNumber, maxAttempts)

    for attempt := 1; attempt <= maxAttempts; attempt++ {
        fmt.Printf("Попытка %d/%d. Введите число: ", attempt, maxAttempts)
        scanner.Scan()
        input := scanner.Text()

        guess, err := strconv.Atoi(input)
        if err != nil {
            fmt.Println("Введите целое число.")
            attempt-- // не считаем как попытку
            continue
        }

        if guess == secret {
            fmt.Printf("🎉 Верно! Вы угадали за %d попыток!\n", attempt)
            return
        } else if guess < secret {
            fmt.Println("Моё число больше!")
        } else {
            fmt.Println("Моё число меньше!")
        }
    }

    fmt.Printf("Попытки закончились. Я загадал %d.\n", secret)
}

func askPlayAgain(scanner *bufio.Scanner) bool {
    for {
        fmt.Print("\nИграть ещё? (yes/no): ")
        scanner.Scan()
        answer := scanner.Text()

        if answer == "yes" {
            return true
        } else if answer == "no" {
            return false
        }
        fmt.Println("Введите 'yes' или 'no'.")
    }
}

func main() {
    scanner := bufio.NewScanner(os.Stdin)
    greet()

    for {
        maxAttempts, maxNumber := chooseDifficulty(scanner)
        playRound(scanner, maxAttempts, maxNumber)
        if !askPlayAgain(scanner) {
            break
        }
        fmt.Println()
    }

    fmt.Println("Спасибо за игру! До встречи!")
}
` + "```" + `

Обратите внимание: это почти готовая игра! Мы собрали все кирпичики из предыдущих уроков в функции.

## Советы по именованию

- Имя функции — **глагол** или **глагольная фраза**: ` + "`greet`" + `, ` + "`chooseDifficulty`" + `, ` + "`playRound`" + `.
- Параметры — **существительные**: ` + "`maxAttempts`" + `, ` + "`scanner`" + `, ` + "`maxNumber`" + `.
- Функция должна делать **одну вещь** — если сложно придумать короткое имя, функция, скорее всего, делает слишком много.

## Итог

- ` + "`func name(params) returnType { ... }`" + ` — объявление функции.
- Функция может принимать параметры и возвращать значения.
- Go поддерживает **множественные возвращаемые значения** — идеально для пары (результат, ошибка).
- Разбивайте код на функции, чтобы ` + "`main()`" + ` читался как план программы.

В следующем уроке — **чеклист** перед проектом: убедимся, что вы готовы.
`,

"4-10-checklist": `# Чеклист: готовы ли вы к проекту

Спринт подходит к концу. Прежде чем перейти к написанию «Угадайки», убедитесь, что владеете всеми необходимыми инструментами. Пройдитесь по чеклисту и честно отметьте, что вы уже умеете.

## Переменные и типы

- [ ] Умею объявлять переменные через ` + "`var`" + ` с указанием типа
- [ ] Умею использовать краткое объявление ` + "`:=`" + `
- [ ] Знаю разницу между ` + "`var`" + ` и ` + "`:=`" + ` (где какой можно использовать)
- [ ] Знаю четыре основных типа: ` + "`int`" + `, ` + "`float64`" + `, ` + "`string`" + `, ` + "`bool`" + `
- [ ] Знаю нулевые значения: ` + "`0`" + `, ` + "`0.0`" + `, ` + "`\"\"`" + `, ` + "`false`" + `
- [ ] Умею объявлять константы через ` + "`const`" + `

**Проверка**: напишите код, который объявляет переменные для игры — имя игрока, количество попыток, загаданное число и флаг «угадано»:

` + "```go" + `
playerName := "Гость"
attempts := 0
secret := 42
isGuessed := false

const maxAttempts = 7
` + "```" + `

## Вывод в консоль

- [ ] Умею использовать ` + "`fmt.Println`" + ` для вывода с переносом строки
- [ ] Умею использовать ` + "`fmt.Print`" + ` для вывода без переноса
- [ ] Умею использовать ` + "`fmt.Printf`" + ` с плейсхолдерами
- [ ] Знаю основные плейсхолдеры: ` + "`%d`" + `, ` + "`%s`" + `, ` + "`%v`" + `
- [ ] Помню, что ` + "`Printf`" + ` не добавляет ` + "`\\n`" + ` автоматически

**Проверка**: выведите сообщение о попытке:

` + "```go" + `
fmt.Printf("Попытка %d/%d. Введите число: ", 3, 7)
` + "```" + `

## Условия

- [ ] Умею писать ` + "`if`" + `/` + "`else if`" + `/` + "`else`" + `
- [ ] Знаю операторы сравнения: ` + "`==`" + `, ` + "`!=`" + `, ` + "`<`" + `, ` + "`>`" + `, ` + "`<=`" + `, ` + "`>=`" + `
- [ ] Знаю логические операторы: ` + "`&&`" + `, ` + "`||`" + `, ` + "`!`" + `
- [ ] Знаю про ` + "`if`" + ` с инициализацией

**Проверка**: напишите сравнение догадки с загаданным числом:

` + "```go" + `
if guess == secret {
    fmt.Println("Угадали!")
} else if guess < secret {
    fmt.Println("Моё число больше!")
} else {
    fmt.Println("Моё число меньше!")
}
` + "```" + `

## Циклы

- [ ] Умею писать цикл со счётчиком: ` + "`for i := 0; i < n; i++`" + `
- [ ] Умею писать цикл с условием (while-стиль): ` + "`for condition`" + `
- [ ] Умею писать бесконечный цикл: ` + "`for { }`" + `
- [ ] Знаю, как работает ` + "`break`" + `
- [ ] Знаю, как работает ` + "`continue`" + `
- [ ] Умею использовать паттерн «бесконечный цикл + break»

**Проверка**: напишите цикл, который запрашивает число от 1 до 3:

` + "```go" + `
for {
    fmt.Print("Выбор (1-3): ")
    // ... чтение ввода ...
    if choice >= 1 && choice <= 3 {
        break
    }
    fmt.Println("Неверный выбор!")
}
` + "```" + `

## Случайные числа

- [ ] Умею импортировать ` + "`math/rand`" + `
- [ ] Знаю, что ` + "`rand.Intn(n)`" + ` возвращает число от 0 до n-1
- [ ] Умею генерировать число в произвольном диапазоне: ` + "`rand.Intn(max) + 1`" + `
- [ ] Знаю, что в Go 1.20+ seed задаётся автоматически

**Проверка**: сгенерируйте число от 1 до 100:

` + "```go" + `
secret := rand.Intn(100) + 1
` + "```" + `

## Чтение ввода

- [ ] Умею создать ` + "`bufio.Scanner`" + ` для чтения из ` + "`os.Stdin`" + `
- [ ] Знаю методы ` + "`Scan()`" + ` и ` + "`Text()`" + `
- [ ] Умею преобразовать строку в число через ` + "`strconv.Atoi`" + `
- [ ] Умею обрабатывать ошибку преобразования (` + "`err != nil`" + `)
- [ ] Умею писать цикл валидации ввода

**Проверка**: прочитайте число с обработкой ошибки:

` + "```go" + `
scanner := bufio.NewScanner(os.Stdin)
scanner.Scan()
input := scanner.Text()

number, err := strconv.Atoi(input)
if err != nil {
    fmt.Println("Не число!")
}
` + "```" + `

## Функции

- [ ] Умею объявлять функции с параметрами
- [ ] Умею возвращать значения из функции
- [ ] Знаю про множественные возвращаемые значения
- [ ] Понимаю, зачем разбивать код на функции

**Проверка**: напишите функцию, которая возвращает параметры сложности:

` + "```go" + `
func getDifficulty(choice int) (int, int) {
    if choice == 1 {
        return 10, 50
    } else if choice == 2 {
        return 7, 100
    }
    return 5, 200
}
` + "```" + `

## Общие навыки

- [ ] Умею запускать программу через ` + "`go run main.go`" + `
- [ ] Умею инициализировать модуль через ` + "`go mod init`" + `
- [ ] Понимаю структуру программы Go: ` + "`package main`" + `, ` + "`import`" + `, ` + "`func main()`" + `

## Если что-то непонятно

Не страшно, если не все пункты отмечены. Вернитесь к соответствующему уроку и **перепишите примеры руками** — не копируйте, а набирайте вручную. Это лучший способ запомнить синтаксис.

Когда все пункты отмечены — вы готовы. В Спринте 5 мы соберём всё в работающую игру. Вперёд!
`,

"5-1-requirement-1": `# Шаг 1: Инициализация проекта и выбор сложности

Начинаем писать «Угадайку». В этом уроке мы создадим проект, напишем приветствие и реализуем выбор уровня сложности. В конце — первый коммит.

## Создаём проект

Откройте терминал и выполните:

` + "```bash" + `
mkdir guessing-game
cd guessing-game
go mod init guessing-game
` + "```" + `

Команда ` + "`go mod init`" + ` создаёт файл ` + "`go.mod`" + ` — он описывает модуль (проект) Go. Для нашей игры этого достаточно.

Создайте файл ` + "`main.go`" + `:

` + "```bash" + `
touch main.go
` + "```" + `

## Полный код шага 1

Откройте ` + "`main.go`" + ` в редакторе и напишите:

` + "```go" + `
package main

import (
	"bufio"
	"fmt"
	"os"
	"strconv"
)

func main() {
	scanner := bufio.NewScanner(os.Stdin)

	// Приветствие
	fmt.Println("=== Добро пожаловать в Угадайку! ===")
	fmt.Println("Я загадаю число, а вы попробуете его угадать.")
	fmt.Println("После каждой попытки я подскажу: больше или меньше.")
	fmt.Println()

	// Выбор сложности
	fmt.Println("Выберите сложность:")
	fmt.Println("1 - Easy   (10 попыток, число от 1 до 50)")
	fmt.Println("2 - Medium (7 попыток, число от 1 до 100)")
	fmt.Println("3 - Hard   (5 попыток, число от 1 до 200)")

	var maxAttempts int
	var maxNumber int

	for {
		fmt.Print("\nВаш выбор: ")
		scanner.Scan()
		input := scanner.Text()

		choice, err := strconv.Atoi(input)
		if err != nil {
			fmt.Println("Пожалуйста, введите число 1, 2 или 3.")
			continue
		}

		if choice == 1 {
			maxAttempts = 10
			maxNumber = 50
			fmt.Println("Режим: Easy")
		} else if choice == 2 {
			maxAttempts = 7
			maxNumber = 100
			fmt.Println("Режим: Medium")
		} else if choice == 3 {
			maxAttempts = 5
			maxNumber = 200
			fmt.Println("Режим: Hard")
		} else {
			fmt.Println("Нет такого уровня. Введите 1, 2 или 3.")
			continue
		}

		break
	}

	fmt.Printf("\nОтлично! У вас %d попыток. Диапазон: 1–%d.\n", maxAttempts, maxNumber)
	fmt.Println("Игровая логика будет в следующем шаге...")
}
` + "```" + `

## Разбор кода

### Импорты

` + "```go" + `
import (
	"bufio"
	"fmt"
	"os"
	"strconv"
)
` + "```" + `

- ` + "`bufio`" + ` — для создания сканера ввода.
- ` + "`fmt`" + ` — для вывода текста.
- ` + "`os`" + ` — для доступа к ` + "`os.Stdin`" + ` (стандартный ввод).
- ` + "`strconv`" + ` — для преобразования строки в число.

### Сканер

` + "```go" + `
scanner := bufio.NewScanner(os.Stdin)
` + "```" + `

Создаём сканер один раз в начале ` + "`main()`" + `. Он будет использоваться для всех операций чтения.

### Цикл выбора

` + "```go" + `
for {
    fmt.Print("\nВаш выбор: ")
    scanner.Scan()
    input := scanner.Text()

    choice, err := strconv.Atoi(input)
    if err != nil {
        fmt.Println("Пожалуйста, введите число 1, 2 или 3.")
        continue
    }
    // ...
    break
}
` + "```" + `

Паттерн «бесконечный цикл + валидация + break» — мы разбирали его в уроках 4-6 и 4-8. Если ввод некорректен, ` + "`continue`" + ` возвращает к началу цикла. Если всё хорошо — ` + "`break`" + ` выходит из него.

## Запускаем

` + "```bash" + `
go run main.go
` + "```" + `

Попробуйте ввести:
- Корректные значения: 1, 2, 3
- Некорректные: 0, 4, ` + "`abc`" + `, пустую строку

Программа должна корректно обрабатывать все случаи и не падать.

## Первый коммит

Инициализируем git-репозиторий и сохраним прогресс:

` + "```bash" + `
git init
git add go.mod main.go
git commit -m "feat: init project, add greeting and difficulty selection"
` + "```" + `

Коммитим рано и часто — это хорошая привычка. Если что-то сломается на следующем шаге, всегда можно вернуться.

## Что мы сделали

- Создали проект с ` + "`go mod init`" + `.
- Написали приветствие с правилами.
- Реализовали выбор сложности с валидацией ввода.
- Определили переменные ` + "`maxAttempts`" + ` и ` + "`maxNumber`" + ` в зависимости от выбора.
- Сделали первый коммит.

## Что дальше

В следующем уроке добавим главное — **игровой цикл**: генерацию числа, ввод догадок и подсказки «больше/меньше».
`,

"5-2-requirement-2": `# Шаг 2: Игровой раунд

В этом уроке мы добавим сердце игры — функцию ` + "`playRound`" + `, которая генерирует случайное число и даёт игроку его угадать. Каждая попытка сопровождается подсказкой.

## Полный код main.go после шага 2

` + "```go" + `
package main

import (
	"bufio"
	"fmt"
	"math/rand"
	"os"
	"strconv"
)

func chooseDifficulty(scanner *bufio.Scanner) (int, int) {
	fmt.Println("Выберите сложность:")
	fmt.Println("1 - Easy   (10 попыток, число от 1 до 50)")
	fmt.Println("2 - Medium (7 попыток, число от 1 до 100)")
	fmt.Println("3 - Hard   (5 попыток, число от 1 до 200)")

	for {
		fmt.Print("\nВаш выбор: ")
		scanner.Scan()
		input := scanner.Text()

		choice, err := strconv.Atoi(input)
		if err != nil {
			fmt.Println("Пожалуйста, введите число 1, 2 или 3.")
			continue
		}

		if choice == 1 {
			return 10, 50
		} else if choice == 2 {
			return 7, 100
		} else if choice == 3 {
			return 5, 200
		}

		fmt.Println("Нет такого уровня. Введите 1, 2 или 3.")
	}
}

func playRound(scanner *bufio.Scanner, maxAttempts, maxNumber int) {
	secret := rand.Intn(maxNumber) + 1
	fmt.Printf("\nЯ загадал число от 1 до %d. У вас %d попыток. Удачи!\n\n", maxNumber, maxAttempts)

	for attempt := 1; attempt <= maxAttempts; attempt++ {
		fmt.Printf("Попытка %d/%d. Введите число: ", attempt, maxAttempts)
		scanner.Scan()
		input := scanner.Text()

		guess, err := strconv.Atoi(input)
		if err != nil {
			fmt.Printf("'%s' — не похоже на число. Попробуйте снова.\n", input)
			attempt-- // не засчитываем попытку
			continue
		}

		if guess < 1 || guess > maxNumber {
			fmt.Printf("Число должно быть от 1 до %d. Попробуйте снова.\n", maxNumber)
			attempt-- // не засчитываем попытку
			continue
		}

		if guess == secret {
			fmt.Printf("\n🎉 Верно! Вы угадали число %d за %d ", secret, attempt)
			if attempt == 1 {
				fmt.Println("попытку!")
			} else if attempt >= 2 && attempt <= 4 {
				fmt.Println("попытки!")
			} else {
				fmt.Println("попыток!")
			}
			return
		} else if guess < secret {
			fmt.Println("Моё число больше!")
		} else {
			fmt.Println("Моё число меньше!")
		}

		if attempt == maxAttempts {
			fmt.Printf("\nПопытки закончились! Я загадал число %d.\n", secret)
		}
	}
}

func main() {
	scanner := bufio.NewScanner(os.Stdin)

	fmt.Println("=== Добро пожаловать в Угадайку! ===")
	fmt.Println("Я загадаю число, а вы попробуете его угадать.")
	fmt.Println("После каждой попытки я подскажу: больше или меньше.")
	fmt.Println()

	maxAttempts, maxNumber := chooseDifficulty(scanner)
	playRound(scanner, maxAttempts, maxNumber)
}
` + "```" + `

## Что изменилось

### 1. Добавили импорт math/rand

` + "```go" + `
import (
	// ...
	"math/rand"
	// ...
)
` + "```" + `

Пакет нужен для генерации случайного числа.

### 2. Вынесли выбор сложности в функцию

` + "```go" + `
func chooseDifficulty(scanner *bufio.Scanner) (int, int) {
    // ...
    return maxAttempts, maxNumber
}
` + "```" + `

Код из шага 1 переехал из ` + "`main()`" + ` в отдельную функцию. Она принимает сканер и возвращает два числа: количество попыток и верхнюю границу диапазона.

Обратите внимание на тип параметра: ` + "`*bufio.Scanner`" + ` — это **указатель** на сканер. Звёздочка ` + "`*`" + ` означает, что функция получает не копию, а ссылку на оригинальный сканер. Пока не углубляемся — просто используйте этот синтаксис при передаче сканера.

### 3. Написали playRound

Разберём ключевые части:

**Генерация числа:**

` + "```go" + `
secret := rand.Intn(maxNumber) + 1
` + "```" + `

` + "`rand.Intn(maxNumber)`" + ` возвращает число от 0 до maxNumber-1, а ` + "`+1`" + ` сдвигает диапазон в 1..maxNumber.

**Цикл попыток:**

` + "```go" + `
for attempt := 1; attempt <= maxAttempts; attempt++ {
    // ...
}
` + "```" + `

Классический цикл со счётчиком. Каждая итерация — одна попытка.

**Обработка некорректного ввода:**

` + "```go" + `
if err != nil {
    fmt.Printf("'%s' — не похоже на число. Попробуйте снова.\n", input)
    attempt-- // не засчитываем попытку
    continue
}
` + "```" + `

Приём ` + "`attempt--`" + ` компенсирует ` + "`attempt++`" + ` в заголовке цикла. В результате некорректный ввод не тратит попытку. Тот же приём используется для проверки диапазона.

**Склонение слова «попытка»:**

` + "```go" + `
if attempt == 1 {
    fmt.Println("попытку!")
} else if attempt >= 2 && attempt <= 4 {
    fmt.Println("попытки!")
} else {
    fmt.Println("попыток!")
}
` + "```" + `

Небольшая деталь, но она делает программу приятнее. В русском языке «1 попытка», «2–4 попытки», «5+ попыток».

**Победа и поражение:**

` + "```go" + `
if guess == secret {
    // ...
    return // выходим из функции — раунд окончен
}
` + "```" + `

При угадывании ` + "`return`" + ` завершает функцию. Если цикл дошёл до конца без ` + "`return`" + ` — игрок проиграл.

### 4. Упростили main()

` + "```go" + `
func main() {
    scanner := bufio.NewScanner(os.Stdin)

    // приветствие...

    maxAttempts, maxNumber := chooseDifficulty(scanner)
    playRound(scanner, maxAttempts, maxNumber)
}
` + "```" + `

Теперь ` + "`main()`" + ` компактная и читается как план: создать сканер, поприветствовать, выбрать сложность, сыграть раунд.

## Запуск и тестирование

` + "```bash" + `
go run main.go
` + "```" + `

Проверьте:
- Выбор каждого уровня сложности.
- Ввод корректного числа — подсказки «больше/меньше».
- Ввод текста вместо числа — сообщение об ошибке, попытка не тратится.
- Ввод числа вне диапазона — аналогичная обработка.
- Победу — угадайте число (можно подсмотреть, добавив временный ` + "`fmt.Println(secret)`" + `).
- Проигрыш — исчерпайте все попытки.

## Коммит

` + "```bash" + `
git add main.go
git commit -m "feat: add playRound with guessing logic and hints"
` + "```" + `

## Что дальше

Игра работает, но только один раунд. В следующем шаге добавим возможность играть повторно, улучшим обработку ввода, создадим README и выпустим версию 1.0.
`,

"5-3-requirement-3": `# Шаг 3: Повторная игра и финальная версия

Финальный шаг! Добавляем внешний цикл «Играть ещё?», доводим обработку ввода, пишем README и ставим тег версии. После этого урока у вас будет законченный проект.

## Финальный код main.go

` + "```go" + `
package main

import (
	"bufio"
	"fmt"
	"math/rand"
	"os"
	"strconv"
	"strings"
)

func chooseDifficulty(scanner *bufio.Scanner) (int, int) {
	fmt.Println("Выберите сложность:")
	fmt.Println("  1 - Easy   (10 попыток, число от 1 до 50)")
	fmt.Println("  2 - Medium (7 попыток, число от 1 до 100)")
	fmt.Println("  3 - Hard   (5 попыток, число от 1 до 200)")

	for {
		fmt.Print("\nВаш выбор: ")
		scanner.Scan()
		input := strings.TrimSpace(scanner.Text())

		choice, err := strconv.Atoi(input)
		if err != nil {
			fmt.Println("Пожалуйста, введите число 1, 2 или 3.")
			continue
		}

		switch choice {
		case 1:
			fmt.Println("Режим: Easy")
			return 10, 50
		case 2:
			fmt.Println("Режим: Medium")
			return 7, 100
		case 3:
			fmt.Println("Режим: Hard")
			return 5, 200
		default:
			fmt.Println("Нет такого уровня. Введите 1, 2 или 3.")
		}
	}
}

func readGuess(scanner *bufio.Scanner, maxNumber int) (int, bool) {
	scanner.Scan()
	input := strings.TrimSpace(scanner.Text())

	if input == "" {
		fmt.Println("Пустой ввод. Введите число.")
		return 0, false
	}

	guess, err := strconv.Atoi(input)
	if err != nil {
		fmt.Printf("'%s' — не похоже на число. Попробуйте снова.\n", input)
		return 0, false
	}

	if guess < 1 || guess > maxNumber {
		fmt.Printf("Число должно быть от 1 до %d.\n", maxNumber)
		return 0, false
	}

	return guess, true
}

func playRound(scanner *bufio.Scanner, maxAttempts, maxNumber int) {
	secret := rand.Intn(maxNumber) + 1
	fmt.Printf("\nЯ загадал число от 1 до %d. У вас %d попыток. Удачи!\n\n", maxNumber, maxAttempts)

	attempt := 0
	for attempt < maxAttempts {
		attempt++
		fmt.Printf("Попытка %d/%d. Введите число: ", attempt, maxAttempts)

		guess, ok := readGuess(scanner, maxNumber)
		if !ok {
			attempt-- // не засчитываем попытку
			continue
		}

		if guess == secret {
			fmt.Printf("\n🎉 Верно! Вы угадали число %d за %d %s\n",
				secret, attempt, pluralAttempts(attempt))
			return
		} else if guess < secret {
			fmt.Println("Моё число больше!")
		} else {
			fmt.Println("Моё число меньше!")
		}
	}

	fmt.Printf("\nПопытки закончились! Я загадал число %d.\n", secret)
}

func pluralAttempts(n int) string {
	if n%10 == 1 && n%100 != 11 {
		return "попытку!"
	} else if n%10 >= 2 && n%10 <= 4 && (n%100 < 10 || n%100 >= 20) {
		return "попытки!"
	}
	return "попыток!"
}

func askPlayAgain(scanner *bufio.Scanner) bool {
	for {
		fmt.Print("\nИграть ещё? (yes/no): ")
		scanner.Scan()
		answer := strings.TrimSpace(strings.ToLower(scanner.Text()))

		switch answer {
		case "yes", "y", "да", "д":
			return true
		case "no", "n", "нет", "н":
			return false
		default:
			fmt.Println("Введите 'yes' или 'no'.")
		}
	}
}

func main() {
	scanner := bufio.NewScanner(os.Stdin)

	fmt.Println("╔══════════════════════════════════╗")
	fmt.Println("║   Добро пожаловать в Угадайку!   ║")
	fmt.Println("╚══════════════════════════════════╝")
	fmt.Println()
	fmt.Println("Я загадаю число, а вы попробуете его угадать.")
	fmt.Println("После каждой попытки я подскажу: больше или меньше.")
	fmt.Println()

	for {
		maxAttempts, maxNumber := chooseDifficulty(scanner)
		playRound(scanner, maxAttempts, maxNumber)

		if !askPlayAgain(scanner) {
			break
		}
		fmt.Println()
	}

	fmt.Println("\nСпасибо за игру! До встречи!")
}
` + "```" + `

## Что изменилось по сравнению с шагом 2

### 1. Добавили пакет strings

` + "```go" + `
import (
	// ...
	"strings"
)
` + "```" + `

Используем ` + "`strings.TrimSpace`" + ` для удаления пробелов и ` + "`strings.ToLower`" + ` для приведения к нижнему регистру.

### 2. Обработка ввода вынесена в readGuess

` + "```go" + `
func readGuess(scanner *bufio.Scanner, maxNumber int) (int, bool) {
    // ...
    return guess, true   // всё OK
    return 0, false      // ошибка ввода
}
` + "```" + `

Функция возвращает ` + "`(число, true)`" + ` при успехе или ` + "`(0, false)`" + ` при ошибке. Это чище, чем обрабатывать ошибки внутри цикла.

Теперь обработка включает:
- Пустой ввод.
- Нечисловой ввод.
- Число вне диапазона.

### 3. Улучшили chooseDifficulty

Заменили цепочку ` + "`if/else if`" + ` на ` + "`switch`" + `:

` + "```go" + `
switch choice {
case 1:
    return 10, 50
case 2:
    return 7, 100
case 3:
    return 5, 200
default:
    fmt.Println("Нет такого уровня.")
}
` + "```" + `

` + "`switch`" + ` в Go — это тот же ` + "`if/else if`" + `, но читается лучше, когда сравниваем одну переменную с несколькими значениями. Обратите внимание: в Go **не нужен** ` + "`break`" + ` в каждом ` + "`case`" + ` — он подразумевается автоматически.

### 4. Добавили pluralAttempts

` + "```go" + `
func pluralAttempts(n int) string {
    if n%10 == 1 && n%100 != 11 {
        return "попытку!"
    } else if n%10 >= 2 && n%10 <= 4 && (n%100 < 10 || n%100 >= 20) {
        return "попытки!"
    }
    return "попыток!"
}
` + "```" + `

Правильное склонение для **любого** числа: «1 попытку», «2 попытки», «5 попыток», «11 попыток», «21 попытку», «22 попытки» и так далее.

### 5. Добавили askPlayAgain

` + "```go" + `
func askPlayAgain(scanner *bufio.Scanner) bool {
    // принимает: yes, y, да, д, no, n, нет, н
}
` + "```" + `

Функция принимает ответ на русском и английском, в любом регистре благодаря ` + "`strings.ToLower`" + `.

### 6. Внешний цикл в main()

` + "```go" + `
for {
    maxAttempts, maxNumber := chooseDifficulty(scanner)
    playRound(scanner, maxAttempts, maxNumber)

    if !askPlayAgain(scanner) {
        break
    }
}
` + "```" + `

Каждая итерация — новый раунд. Если игрок отвечает «нет», ` + "`break`" + ` завершает цикл.

## Создаём README.md

Создайте файл ` + "`README.md`" + ` в корне проекта:

` + "```" + `
# Угадайка

Консольная игра «Угадай число» на Go.

## Правила

Компьютер загадывает число, а вы пытаетесь его угадать.
После каждой попытки программа подсказывает: загаданное число больше или меньше.

## Уровни сложности

| Уровень | Попытки | Диапазон |
|---------|---------|----------|
| Easy    | 10      | 1–50     |
| Medium  | 7       | 1–100    |
| Hard    | 5       | 1–200    |

## Запуск

go run main.go

## Требования

Go 1.20+
` + "```" + `

## Финальный коммит и тег

` + "```bash" + `
git add main.go README.md
git commit -m "feat: add play again loop, input handling, README"
` + "```" + `

Теперь ставим тег версии:

` + "```bash" + `
git tag v1.0.0
` + "```" + `

Тег ` + "`v1.0.0`" + ` — это метка на коммите. Она означает: «вот здесь первая стабильная версия». Можно посмотреть историю:

` + "```bash" + `
git log --oneline
` + "```" + `

Вы увидите что-то вроде:

` + "```" + `
a3f2d1c (HEAD -> main, tag: v1.0.0) feat: add play again loop, input handling, README
b1e4c7a feat: add playRound with guessing logic and hints
9d8f3e2 feat: init project, add greeting and difficulty selection
` + "```" + `

Три коммита — три шага. Каждый добавляет новую функциональность, и проект работает после каждого коммита.

## Что мы построили

Поздравляем! Вы написали свой первый проект на Go. Подведём итог:

- **Приветствие** — программа объясняет правила.
- **Выбор сложности** — три уровня с разными параметрами.
- **Генерация случайного числа** — ` + "`math/rand`" + `.
- **Цикл попыток** — ввод, валидация, подсказки.
- **Обработка ошибок** — некорректный ввод не ломает программу.
- **Повторная игра** — внешний цикл с вопросом «Ещё?».
- **Git** — три коммита и тег версии.

Каждый из этих элементов вы изучили в Спринте 4 и применили здесь на практике. Так работает обучение: теория, а затем — проект, где всё соединяется.
`,
	}
	for slug, content := range lessons {
		db.Model(&models.Lesson{}).Where("slug = ?", slug).Update("content", content)
	}
}
