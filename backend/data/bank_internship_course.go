package data

import "golanger/backend/models"

func bankInternshipLessons() []models.Lesson {
	return []models.Lesson{
		bankLesson(1, "Спринт 0 · Project ZERO", "Эксперимент", "atlas-first-day", "Тебя взяли в Project ZERO", "Познакомься с экспериментом банка, командой и правилами стажировки.", `
# День 1. Пропуск уже напечатан

Ты пришёл на обычное собеседование, но оказался в эксперименте крупнейшего банка: **Project ZERO → Engineer**. Руководство хочет проверить, можно ли взять человека без опыта и довести до уровня junior backend developer.

Кроме тебя в группе Данил, который решает всё через AI, и Ленар, который боится даже терминала.

:::chatdialog
заголовок: #zero-onboarding
канал: Backend Pride

left|Рома, тимлид|Здесь не будет лекций на неделю вперёд. Получаешь задачу, узнаёшь необходимое и сразу делаешь.|avatar=teamlead
left|Аркадий Борисович|Через пять спринтов мне нужен работающий результат, а не конспект.|avatar=manager
right|Ты|Что считается результатом первого дня?
left|Рома, тимлид|Понять команду, продукт и свою первую задачу.
:::

## Что нужно сделать руками

1. Создай папку 'zero-engineer'.
2. Внутри создай 'internship.md'.
3. Запиши роли: PM, analyst, backend, QA, designer.
4. Для каждой роли добавь один вопрос, который ты задашь перед началом задачи.

:::choice
вопрос: Кто уточняет бизнес-требования и разбивает их на понятные правила?
- QA
- Аналитик ✓
- Designer
- DevOps
:::

**Результат:** у тебя есть карта команды и первый артефакт стажировки.
`),
		bankLesson(2, "Спринт 0 · Project ZERO", "Рабочая задача", "atlas-product-thinking", "Почему банк платит за backend", "Свяжи код с пользовательским сценарием, риском и бизнес-результатом.", `
# Код начинается не с кода

Аркадий Борисович показывает жалобу: операторы тратят по пять минут, чтобы вручную проверить лимит перевода клиента. Команде нужен внутренний инструмент, который отвечает за секунду.

## Минимальная теория

Backend принимает данные, применяет бизнес-правила и возвращает результат. Хорошая задача описывает:

- проблему пользователя;
- входные данные;
- ожидаемый результат;
- критерии готовности.

## Практика

Открой 'internship.md' и оформи user story:

> Как оператор поддержки, я хочу проверить доступный лимит клиента, чтобы быстро ответить на обращение.

Добавь acceptance criteria:

1. инструмент принимает ID клиента и сумму;
2. отвечает 'approved' или 'rejected';
3. объясняет причину отказа;
4. некорректный ввод не ломает программу.

:::freetext
вопрос: Какой результат увидит оператор, когда задача действительно готова?
подсказка: Опиши наблюдаемое поведение, а не количество написанного кода.
:::

**Типичная ошибка:** начать выбирать framework до того, как понятен сценарий.

**Результат:** готов короткий бриф Project 1.
`),
		bankLesson(3, "Спринт 0 · Project ZERO", "Рабочий процесс", "atlas-first-ticket", "ZERO-1: разбери первую Jira-задачу", "Разложи банковскую фичу на шаги и собери definition of done.", `
# ZERO-1 · Проверка лимита перевода

Юля переносит карточку в 'To Do' и просит оценить задачу. Ленар отвечает: «Я ещё Go не знаю, как оценивать?» Рома предлагает оценивать не строки кода, а неизвестность и шаги.

## Что нужно сделать

Создай 'ZERO-1.md':

~~~text
Goal:
Inputs:
Business rules:
Error cases:
Definition of done:
Questions:
~~~

Заполни документ. Разбей работу максимум на шаги по 30–60 минут:

1. запустить пустую Go-программу;
2. принять данные клиента;
3. проверить бизнес-правила;
4. вывести решение;
5. проверить ошибки вручную.

:::sort
вопрос: Собери рабочий порядок:
1. Уточнить критерии готовности
2. Выделить неизвестные
3. Сделать минимальный happy path
4. Проверить ошибочные сценарии
5. Показать результат команде
:::

**Debugging moment:** если шаг нельзя проверить отдельно, он слишком большой.

**Результат:** задача готова к работе, а не просто лежит в Jira.
`),
		bankLesson(4, "Спринт 0 · Project ZERO", "Sprint Challenge", "atlas-terminal-git", "Онбординг завершён: подготовь рабочее место", "Настрой Go, терминал и репозиторий для первого локального проекта.", `
# Sprint Challenge · рабочее место стажёра

Рома не просит показать сертификаты. Он просит открыть терминал и доказать, что среда готова.

## Сделай руками

~~~bash
mkdir zero-engineer
cd zero-engineer
go mod init bank.local/zero-engineer
go version
go env GOMOD
~~~

Создай 'main.go':

~~~go
package main

import "fmt"

func main() {
	fmt.Println("Project ZERO: workstation ready")
}
~~~

Запусти 'go run .'. Затем намеренно удали кавычку, прочитай ошибку компилятора и исправь её.

## Definition of done

- 'go run .' завершается без ошибки;
- ты можешь объяснить назначение 'go.mod';
- в папке лежат 'internship.md', 'ZERO-1.md', 'main.go';
- следующий шаг записан в 'README.md'.

## Review

Что было неизвестно до начала? Какую ошибку ты теперь умеешь читать?

## Retrospective

- Continue: маленькие проверяемые шаги.
- Stop: пытаться запомнить всё заранее.
- Start: фиксировать команды и ошибки в README.

**Deliverable:** готовое локальное окружение Project 1.
`),

		bankLesson(11, "Спринт 1 · Limit Guard CLI", "Go по необходимости", "go-program-map", "Первый сервис начинается с main", "Собери минимальный CLI-инструмент проверки банковского лимита.", `
# Project 1 · Limit Guard

Операторам нужен локальный прототип сегодня. Никаких серверов и баз данных: один запускаемый файл и понятный ответ.

## Минимальная теория

- 'package main' делает пакет запускаемым;
- 'func main()' — точка входа;
- 'import' подключает нужные пакеты;
- функция должна делать одну понятную часть работы.

## Сделай руками

Создай функцию:

~~~go
func checkLimit(limit, amount int) bool {
	return amount <= limit
}
~~~

Вызови её из 'main()' и выведи решение оператору.

:::interactive lang=go
заголовок: Первый happy path
package main

import "fmt"

func checkLimit(limit, amount int) bool {
	return amount <= limit
}

func main() {
	fmt.Println(checkLimit(100000, 45000))
}
:::

**Mini challenge:** переименуй значения так, чтобы код читался без комментариев.

**Результат:** работающий happy path без лишней архитектуры.
`),
		bankLesson(12, "Спринт 1 · Limit Guard CLI", "Ввод и данные", "go-values-input", "Прими данные оператора безопасно", "Научись читать ввод, хранить состояние и валидировать сумму.", `
# Ввод из реального мира

Юджин вводит вместо суммы слово 'много'. Прототип молча использует ноль. «Поздравляю, — говорит она, — ты только что одобрил подозрительный перевод».

## Минимальная теория

'fmt.Scan' возвращает количество прочитанных значений и ошибку. Любой внешний ввод нужно считать недоверенным.

## Сделай руками

~~~go
var clientID string
var limit, amount int

fmt.Print("Client ID: ")
if _, err := fmt.Scan(&clientID); err != nil {
	fmt.Println("invalid client id")
	return
}
~~~

Добавь чтение лимита и суммы. Отклоняй:

- пустой ID;
- сумму '<= 0';
- лимит '< 0';
- ввод неправильного типа.

:::code lang=go
вопрос: Добавь проверку, которая завершает программу при amount <= 0.
~~~go
if amount <= 0 {
	fmt.Println("amount must be positive")
	return
}
~~~
:::

**Debugging moment:** проверь ввод 'abc', '-10', '0' и очень большую сумму.

**Результат:** CLI не принимает заведомо некорректные данные.
`),
		bankLesson(13, "Спринт 1 · Limit Guard CLI", "Бизнес-правила", "go-conditions", "Одобрить, отклонить, объяснить", "Реализуй ветвление бизнес-решения и понятные причины отказа.", `
# Правило должно быть видно в коде

Снежана приносит уточнение: перевод отклоняется, если сумма выше лимита или выше разового порога '150000'.

## Минимальная теория

'if' выбирает ветку по логическому условию. Порядок проверок важен: сначала ошибки входа, затем бизнес-ограничения, потом успешный результат.

## Практика

Создай:

~~~go
func decision(limit, amount int) (string, string) {
	if amount > 150000 {
		return "rejected", "single transfer limit exceeded"
	}
	if amount > limit {
		return "rejected", "client limit exceeded"
	}
	return "approved", "within limit"
}
~~~

Проверь таблицу:

| limit | amount | result |
|---:|---:|---|
| 100000 | 50000 | approved |
| 100000 | 120000 | rejected |
| 300000 | 160000 | rejected |

**Mini challenge:** не дублируй вывод в каждой ветке — верни решение из функции.

**Типичная ошибка:** смешать чтение ввода, правила и печать в одном большом 'main'.

**Результат:** бизнес-правила отделены и объяснимы.
`),
		bankLesson(14, "Спринт 1 · Limit Guard CLI", "Sprint Challenge", "number-guessing-release", "Релиз Project 1: локальный Limit Guard", "Доведи локальный CLI до состояния, которое можно показать оператору и тимлиду.", `
# Sprint Challenge · Project 1

Аркадий Борисович даёт десять минут: показать, как инструмент экономит время оператора.

## Собери релиз

Программа должна:

1. принимать ID клиента, лимит и сумму;
2. валидировать ввод;
3. применять два бизнес-правила;
4. выводить статус и причину;
5. позволять проверить несколько клиентов за один запуск.

Добавь цикл и команду 'exit'. Проверь минимум пять сценариев из 'test-cases.md'.

## Code review от Ромы

- функции названы по смыслу;
- 'main' связывает шаги, а не содержит всю логику;
- ошибки понятны оператору;
- нет функций «на будущее».

:::choice
вопрос: Что доказывает готовность Project 1?
- Код компилируется один раз
- Все критерии и тестовые сценарии выполнены ✓
- В проекте больше 200 строк
- Использован framework
:::

## Retrospective

Запиши: какую ошибку ты нашёл, как воспроизвёл и какой минимальный фикс сделал.

**Deliverable:** локальный CLI 'limit-guard', README и таблица ручных тестов.

**What Student Learned:** функции, ввод, условия, циклы, валидация и debugging.
`),

		bankLesson(21, "Спринт 2 · Release Pipeline", "Git и команда", "network-addresses", "Первый командный репозиторий", "Перенеси Project 1 в Git и научись делать проверяемые изменения.", `
# Project 2 · Команда должна получить твой код

Данил присылает архив 'final-final-2.zip'. Юля отвечает: «Мы не можем ревьюить архивы». Твоя задача — подготовить нормальный Git-репозиторий.

## Минимальная теория

Commit — законченная проверяемая мысль. Branch изолирует задачу. Pull Request показывает изменение и контекст ревью.

## Сделай руками

~~~bash
git init
git add go.mod main.go README.md
git commit -m "add limit guard happy path"
git switch -c feature/input-validation
~~~

Добавь '.gitignore', раздели текущую работу минимум на два осмысленных коммита и просмотри:

~~~bash
git status
git diff
git log --oneline
~~~

**Mini challenge:** объясни каждый коммит одной фразой без слова «изменения».

**Результат:** история проекта понятна другому разработчику.
`),
		bankLesson(22, "Спринт 2 · Release Pipeline", "GitHub и review", "network-ports", "Pull Request без паники", "Опубликуй ветку, оформи PR и отработай замечание тимлида.", `
# Рома оставил комментарий

В PR он пишет: «Причины отказа лучше оформить константами». Это не оценка тебя — это запрос сделать код устойчивее.

## Практика

~~~bash
git remote add origin <repository-url>
git push -u origin feature/input-validation
~~~

Оформи Pull Request:

- Problem;
- What changed;
- How to test;
- Risks;
- Screenshot или пример CLI-сессии.

После замечания:

1. воспроизведи проблему;
2. внеси минимальный фикс;
3. запусти проект;
4. сделай отдельный commit;
5. ответь, что и как проверил.

:::freetext
вопрос: Как бы ты ответил на замечание «обработка ошибки скрывает причину»?
подсказка: Спокойно опиши фикс и способ проверки.
:::

**Типичная ошибка:** переписать весь проект во время маленького review.

**Результат:** открытый PR с понятным сценарием проверки.
`),
		bankLesson(23, "Спринт 2 · Release Pipeline", "Docker", "http-request-response", "Упакуй сервис одинаково для всех", "Создай Dockerfile и запусти CLI в воспроизводимом окружении.", `
# «У меня работает» больше не аргумент

На ноутбуке QA другая версия Go. Юджин не может повторить твой запуск. Рома предлагает зафиксировать окружение в Docker.

## Минимальная теория

Image — шаблон приложения. Container — запущенный экземпляр. Dockerfile описывает воспроизводимую сборку.

## Сделай руками

~~~dockerfile
FROM golang:1.24-alpine AS build
WORKDIR /app
COPY go.mod ./
COPY . .
RUN go build -o limit-guard .

FROM alpine:3.21
WORKDIR /app
COPY --from=build /app/limit-guard .
ENTRYPOINT ["./limit-guard"]
~~~

~~~bash
docker build -t limit-guard:dev .
docker run --rm -it limit-guard:dev
~~~

Добавь '.dockerignore'. Сравни размер build-stage и финального image.

**Debugging moment:** намеренно укажи неверный путь в 'COPY', прочитай слой, на котором упала сборка, исправь.

**Результат:** проект запускается одной Docker-командой.
`),
		bankLesson(24, "Спринт 2 · Release Pipeline", "Sprint Challenge", "weather-api-release", "Релиз Project 2: GitHub + Docker", "Проведи проект через branch, PR, review и контейнерный запуск.", `
# Sprint Challenge · передача проекта команде

Юля ставит дедлайн 17:00. До него QA должна получить воспроизводимый релиз.

## Deliverable

- GitHub-репозиторий без секретов и бинарников;
- feature branch и Pull Request;
- минимум одно обработанное review-замечание;
- multi-stage Dockerfile;
- README с командами build/run;
- тег 'v0.2.0'.

## Проверка QA

Новый человек должен клонировать проект и запустить его, используя только README.

:::sort
вопрос: Порядок безопасного релиза:
1. Проверить git diff
2. Запустить локальные сценарии
3. Собрать Docker image
4. Проверить контейнер
5. Открыть Pull Request
6. Получить review
7. Поставить тег
:::

## Retrospective

- Что сломалось только в Docker?
- Какой комментарий review улучшил продукт?
- Что ты автоматизируешь в следующем спринте?

**What Student Learned:** Git, GitHub flow, code review, Docker image и воспроизводимый запуск.
`),

		bankLesson(31, "Спринт 3 · Transfer Rules API", "REST API", "go-structs", "Первый endpoint банка", "Преврати локальную проверку лимита в HTTP API с JSON-контрактом.", `
# Project 3 · Limit Guard становится сервисом

Юра делает интерфейс оператора и больше не может запускать твой CLI. Ему нужен endpoint, куда frontend отправит данные.

## Минимальная теория

REST API принимает HTTP-запрос и возвращает статус и JSON. Контракт важнее внутренней реализации.

## Сделай руками

Опиши модели:

~~~go
type CheckRequest struct {
	ClientID string // JSON field: clientId
	Amount   int64  // JSON field: amount
}

type CheckResponse struct {
	Status string // JSON field: status
	Reason string // JSON field: reason
}
~~~

Создай 'POST /api/v1/limits/check' через 'net/http'. Декодируй JSON, вызови бизнес-функцию, закодируй ответ.

**Mini challenge:** верни '400', если JSON повреждён, и '200' для валидного бизнес-отказа.

**Результат:** frontend может вызвать первый endpoint.
`),
		bankLesson(32, "Спринт 3 · Transfer Rules API", "Postman", "go-slices-maps", "Зафиксируй API-контракт в Postman", "Проверь happy path, ошибки и статус-коды как настоящий потребитель API.", `
# QA не верит скриншоту терминала

Юджин просит коллекцию запросов, которую можно повторить. Postman становится исполняемым описанием контракта.

## Сделай руками

Создай коллекцию 'Limit Guard API':

1. approved request;
2. client limit exceeded;
3. invalid JSON;
4. missing clientId;
5. non-positive amount.

Для каждого запроса сохрани:

- method и URL;
- headers;
- body;
- ожидаемый status code;
- пример response.

Добавь test:

~~~javascript
pm.test("status is present", function () {
  const body = pm.response.json();
  pm.expect(body.status).to.be.a("string");
});
~~~

**Типичная ошибка:** всегда возвращать '200', даже когда запрос технически некорректен.

**Результат:** экспортированная Postman collection лежит в 'docs/'.
`),
		bankLesson(33, "Спринт 3 · Transfer Rules API", "PostgreSQL", "crud-lifecycle", "Сохрани правила клиентов в PostgreSQL", "Подключи базу, создай схему и замени значения из кода реальными данными.", `
# Лимиты нельзя хранить в исходниках

Снежана сообщает: правила меняются ежедневно. Пересобирать сервис ради нового лимита нельзя.

## Минимальная теория

Таблица хранит сущности, primary key идентифицирует запись, query получает нужные данные. Параметры запроса защищают от SQL injection.

## Сделай руками

~~~sql
CREATE TABLE client_limits (
    client_id TEXT PRIMARY KEY,
    daily_limit BIGINT NOT NULL CHECK (daily_limit >= 0),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
~~~

Подними PostgreSQL в Docker, добавь три тестовых клиента и реализуй:

~~~sql
SELECT daily_limit FROM client_limits WHERE client_id = $1;
~~~

Обработай отдельно:

- клиент найден;
- клиент не найден;
- база недоступна.

**Debugging moment:** останови контейнер PostgreSQL и убедись, что API возвращает контролируемую '503', а не зависает.

**Результат:** endpoint получает лимит из PostgreSQL.
`),
		bankLesson(34, "Спринт 3 · Transfer Rules API", "Sprint Challenge", "expense-tracker-release", "Релиз Project 3: PostgreSQL REST API", "Собери банковский API, проверь его Postman-коллекцией и подготовь к демо.", `
# Sprint Challenge · Transfer Rules API

Аркадий Борисович хочет увидеть полный путь: запрос оператора → backend → PostgreSQL → бизнес-решение → JSON.

## Deliverable

- 'POST /api/v1/limits/check';
- JSON validation и понятные ошибки;
- PostgreSQL migration;
- параметризованный query;
- Docker Compose для API и базы;
- Postman collection минимум с пятью сценариями;
- README с запуском одной командой.

## Definition of done

~~~bash
docker compose up --build
~~~

После запуска все Postman-проверки проходят на чистой машине.

## Module Review

Объясни вслух:

1. чем '400' отличается от бизнес-отказа;
2. зачем query использует '$1';
3. что произойдёт при недоступной БД;
4. где заканчивается transport и начинается business logic.

## Retrospective

Запиши один технический долг, один риск и одно улучшение следующего спринта.

**What Student Learned:** REST, JSON, status codes, Postman, PostgreSQL и интеграционный debugging.
`),

		bankLesson(41, "Спринт 4 · Production Review", "Тестирование", "testing-mindset", "Юджин ломает твой API", "Покрой критические правила тестами и научись находить границы до QA.", `
# День разрушения

Юджин отправляет сумму '150001', неизвестного клиента и двадцать запросов одновременно. Один сценарий возвращает неправильную причину.

## Минимальная теория

Тест проверяет поведение: given → when → then. Для бизнес-правил удобны table-driven tests.

## Сделай руками

Создай таблицу сценариев для 'decision':

~~~go
tests := []struct {
	name       string
	limit      int64
	amount     int64
	wantStatus string
}{
	{"within limit", 100000, 50000, "approved"},
	{"over client limit", 100000, 120000, "rejected"},
	{"over single limit", 300000, 150001, "rejected"},
}
~~~

Добавь handler test через 'httptest' и интеграционный smoke test с PostgreSQL.

**Mini challenge:** сначала напиши тест, который воспроизводит найденный баг, затем фикс.

**Результат:** критические правила защищены автоматическими тестами.
`),
		bankLesson(42, "Спринт 4 · Production Review", "Надёжность", "code-review", "Timeout, context и честные ошибки", "Останови зависающие запросы и добавь диагностируемое поведение сервиса.", `
# Запрос висит 30 секунд

Рома показывает production-график: при проблеме с БД handler продолжает ждать, а оператор нажимает кнопку снова.

## Минимальная теория

'context.Context' переносит deadline и cancellation. Ошибки нужно оборачивать с контекстом, но не раскрывать внутренности клиенту.

## Сделай руками

~~~go
ctx, cancel := context.WithTimeout(r.Context(), 2*time.Second)
defer cancel()

limit, err := repo.FindLimit(ctx, clientID)
~~~

Добавь:

- timeout для database query;
- '503 Service Unavailable' при временной ошибке;
- request ID в лог;
- структурированный лог без персональных данных.

**Типичная ошибка:** логировать весь request body с клиентскими данными.

**Debugging moment:** искусственно задержи repository на три секунды и проверь отмену.

**Результат:** сервис не зависает бесконечно и оставляет полезный след для расследования.
`),
		bankLesson(43, "Спринт 4 · Production Review", "Demo Day", "demo-day", "Покажи продукт, а не код", "Подготовь короткое демо от бизнес-проблемы до проверенного результата.", `
# Demo Day

В комнате Рома, Юля, Снежана, Юджин и Аркадий Борисович. У тебя семь минут.

## Сценарий демо

1. Проблема: оператор медленно проверяет лимит.
2. Контракт: frontend отправляет запрос.
3. Happy path: перевод разрешён.
4. Business rejection: лимит превышен.
5. Technical failure: база остановлена, API честно отвечает '503'.
6. Доказательства: Postman collection и тесты.
7. Следующий шаг: аудит решений или rate limiting.

## Сделай руками

Создай 'DEMO.md' и подготовь чистый запуск:

~~~bash
docker compose down -v
docker compose up --build
go test ./...
~~~

:::freetext
вопрос: Какую бизнес-ценность дал сервис?
подсказка: Говори про время оператора, ошибки и воспроизводимость, а не про количество endpoint.
:::

**Результат:** воспроизводимое семиминутное демо с понятным impact.
`),
		bankLesson(44, "Спринт 4 · Production Review", "Оффер", "offer-retro", "Финальная ретроспектива Project ZERO", "Собери доказательства навыков, пройди review и определи следующий уровень роста.", `
# Итог эксперимента

Аркадий Борисович закрывает ноутбук: «Нас интересовало не то, сколько терминов ты знаешь. Нас интересовало, можешь ли ты довести задачу до работающего результата».

## Финальный review

Проверь портфолио стажировки:

- Project 1: локальный CLI и ручные тесты;
- Project 2: GitHub flow, PR и Docker;
- Project 3: REST API, PostgreSQL, Postman и Docker Compose;
- tests, timeout, logs и demo script.

## Reflection

Ответь письменно:

1. Как ты выясняешь требования?
2. Как делишь задачу?
3. Как проверяешь happy path и ошибки?
4. Как доказываешь, что сервис запускается у другого человека?
5. Как реагируешь на code review?

## Retrospective

- Keep: привычка делать маленький проверяемый результат.
- Improve: один навык, который пока требует подсказки.
- Next: одна backend-фича на ближайшие две недели.

:::form
заголовок: Твой следующий sprint goal
поле: Какой backend-результат ты выпустишь следующим
placeholder: Например: добавлю авторизацию и интеграционные тесты
кнопка: Зафиксировать
успех: Цель сохранена. Project ZERO завершён, инженерный путь продолжается.
:::

**Deliverable:** три проекта, история решений и доказательства junior backend skills.

**Story Moment:** Данил признаёт, что AI не заменяет проверку результата. Ленар впервые сам объясняет ошибку в логах. Ты выходишь из эксперимента не «человеком без опыта», а разработчиком с завершёнными проектами.
`),
	}
}

func bankLesson(order int, module, category, slug, title, description, content string) models.Lesson {
	return flagshipLesson(order, module, category, slug, title, description, content)
}
