package data

import (
	"golanger/backend/models"
	"gorm.io/gorm"
)

func SeedModule1Content(db *gorm.DB) {
	lessons := map[string]string{

		// ── Спринт 1. Введение в разработку ──

		"1-1-how-course-works": `# Как устроен курс

Привет! Ты начинаешь курс **Godemy** — практический маршрут по Go с нуля до первых проектов в портфолио.

---

## Формат обучения

Курс разбит на **7 модулей** и **12 спринтов**. Каждый спринт — это набор уроков с теорией и практикой.

| Часть | Что внутри |
|-------|-----------|
| Урок | Короткая теория + примеры кода |
| Тренажёр | Задачи с автопроверкой в браузере |
| Проект | Пошаговая сборка реального приложения |

---

## Как проходить

1. **Читай урок** — каждый занимает 10–20 минут
2. **Решай задачи** — закрепляй тему в тренажёре
3. **Собирай проект** — коммит за коммитом, как в реальной работе

Не нужно запоминать всё сразу. Важнее **пройти и попробовать**, а потом вернуться к непонятному.

---

## Что ты получишь

- 3 проекта на GitHub с README
- Навыки Go, HTTP, SQL, Docker
- Готовое резюме для junior-вакансий

---

## Правила

- **Свой темп** — нет дедлайнов и расписания группы
- **Прогресс сохраняется** — можно вернуться в любой момент
- **Вопросы** — задавай в Telegram-чате курса

Давай начнём! Следующий урок — почему именно Go.
`,

		"1-2-why-go": `# Почему Go

Go (Golang) — язык программирования, созданный в Google в 2007 году. Его авторы — Роб Пайк, Кен Томпсон и Роберт Гриземер.

---

## Зачем создали Go

В Google писали огромные проекты на C++ и Java. Компиляция занимала минуты, код был сложным. Нужен был язык, который:

- **Компилируется быстро** — секунды, не минуты
- **Читается легко** — минимум магии и неявных конструкций
- **Работает быстро** — компилируется в нативный бинарник
- **Поддерживает конкурентность** — goroutines встроены в язык

---

## Сравнение с другими языками

| | Go | Python | Java | C++ |
|---|---|---|---|---|
| Скорость работы | Быстрый | Медленный | Средний | Быстрый |
| Скорость компиляции | Секунды | Не нужна | Медленная | Медленная |
| Простота | Простой | Простой | Сложный | Очень сложный |
| Конкурентность | Встроена | Библиотеки | Сложная | Сложная |
| Бинарник | Один файл | Нужен интерпретатор | Нужна JVM | Один файл |

---

## Где используют Go

- **Backend API** — REST, gRPC, микросервисы
- **DevOps-инструменты** — Docker, Kubernetes, Terraform
- **Высоконагруженные системы** — очереди, стриминг, прокси
- **CLI-утилиты** — быстрые инструменты командной строки

---

## Рынок труда

Go-разработчики востребованы в Яндекс, Ozon, Wildberries, VK, T-Bank и сотнях других компаний. Зарплаты junior Go-разработчиков начинаются от **80 000–120 000 ₽** в России.

Go — это не просто язык. Это вход в экосистему, где пишутся самые современные backend-системы.
`,

		"1-3-frontend-backend": `# Что такое фронтенд и бекенд

Когда ты открываешь приложение — заказываешь еду, смотришь погоду или пишешь в мессенджер — за этим стоят две части: **фронтенд** и **бекенд**.

---

## Фронтенд — то, что видит пользователь

Фронтенд — это интерфейс: кнопки, формы, анимации, страницы. Всё, с чем взаимодействует человек.

Технологии: HTML, CSS, JavaScript, React, Vue.

---

## Бекенд — то, что работает на сервере

Бекенд — это логика: обработка запросов, работа с базой данных, авторизация, отправка email.

Технологии: Go, Python, Java, Node.js, PostgreSQL, Redis.

---

## Как они общаются

` + "```" + `
Браузер (фронтенд)           Сервер (бекенд)
       |                           |
       |   GET /api/weather?city=  |
       | ────────────────────────> |
       |                           |  запрос к БД
       |                           |  обработка
       |   { "temp": 22, ... }     |
       | <──────────────────────── |
       |                           |
` + "```" + `

1. Браузер отправляет **HTTP-запрос** на сервер
2. Сервер обрабатывает запрос, берёт данные из БД
3. Сервер отправляет **ответ** (обычно в формате JSON)
4. Браузер показывает данные пользователю

---

## Наша роль

На этом курсе ты учишься быть **бекенд-разработчиком на Go**. Ты будешь:

- Принимать запросы от фронтенда
- Обрабатывать данные и бизнес-логику
- Работать с базами данных
- Отдавать результат в формате JSON

Фронтенд тебе писать не нужно — но важно понимать, как он общается с твоим бекендом.
`,

		"1-4-go-ecosystem": `# Экосистема Go

Go — не просто язык для учебных проектов. На нём написаны инструменты, которыми пользуется весь мир.

---

## Известные проекты на Go

### Docker
Контейнеризация — способ упаковать приложение со всеми зависимостями. Docker написан на Go и изменил то, как мы деплоим софт.

### Kubernetes (K8s)
Оркестрация контейнеров. Управляет тысячами контейнеров в продакшене. Создан в Google, написан на Go.

### Terraform
Инфраструктура как код. Описываешь сервера, базы и сети в файлах — Terraform создаёт их автоматически.

### Prometheus
Мониторинг и алертинг. Собирает метрики с сервисов и показывает, что происходит в системе.

---

## Почему они выбрали Go

| Причина | Подробнее |
|---------|----------|
| Единый бинарник | Один файл без зависимостей — легко деплоить |
| Быстрая компиляция | Минуты ожидания превращаются в секунды |
| Горутины | Тысячи параллельных задач без сложного кода |
| Строгая типизация | Ошибки ловятся на этапе компиляции |
| Стандартная библиотека | HTTP-сервер, JSON, криптография — всё из коробки |

---

## Компании, которые используют Go

- **Google** — создатели языка
- **Uber** — высоконагруженные микросервисы
- **Twitch** — стриминг в реальном времени
- **Cloudflare** — CDN и сетевая безопасность
- **Яндекс, Ozon, Wildberries, VK** — backend-сервисы

Когда ты учишь Go, ты заходишь в экосистему, которая используется в индустрии прямо сейчас.
`,

		"1-5-terminal-basics": `# Основы работы в терминале

Терминал (командная строка) — основной инструмент разработчика. Через него ты будешь запускать Go-программы, работать с Git и управлять файлами.

---

## Как открыть терминал

- **macOS**: Cmd + Пробел → Terminal (или iTerm2)
- **Linux**: Ctrl + Alt + T
- **Windows**: Win + R → cmd, или PowerShell, или Git Bash

---

## Навигация

| Команда | Что делает | Пример |
|---------|-----------|--------|
| ` + "`pwd`" + ` | Показать текущую папку | ` + "`pwd`" + ` → /Users/alex |
| ` + "`ls`" + ` | Список файлов в папке | ` + "`ls`" + ` → main.go  README.md |
| ` + "`ls -la`" + ` | Подробный список (с скрытыми) | Показывает права, размер, дату |
| ` + "`cd folder`" + ` | Перейти в папку | ` + "`cd projects`" + ` |
| ` + "`cd ..`" + ` | Перейти на уровень выше | ` + "`cd ..`" + ` |
| ` + "`cd ~`" + ` | Перейти в домашнюю папку | ` + "`cd ~`" + ` |

---

## Работа с файлами и папками

| Команда | Что делает |
|---------|-----------|
| ` + "`mkdir myproject`" + ` | Создать папку |
| ` + "`touch main.go`" + ` | Создать пустой файл |
| ` + "`rm file.go`" + ` | Удалить файл |
| ` + "`rm -r folder`" + ` | Удалить папку с содержимым |
| ` + "`cp a.go b.go`" + ` | Скопировать файл |
| ` + "`mv old.go new.go`" + ` | Переименовать / переместить |
| ` + "`cat main.go`" + ` | Показать содержимое файла |

---

## Практика

Попробуй выполнить в терминале:

` + "```bash" + `
mkdir go-practice
cd go-practice
touch main.go
ls
pwd
` + "```" + `

Ты создал папку, перешёл в неё, создал файл и убедился, что всё на месте. Этими командами ты будешь пользоваться каждый день.
`,

		"1-6-install-go": `# Установка Go и настройка IDE

Чтобы писать и запускать Go-программы, нужны две вещи: сам Go и редактор кода.

---

## Установка Go

### 1. Скачай Go

Перейди на [go.dev/dl](https://go.dev/dl/) и скачай установщик для своей ОС.

### 2. Установи

- **macOS**: открой .pkg файл и следуй инструкциям
- **Linux**: распакуй архив в /usr/local
- **Windows**: запусти .msi установщик

### 3. Проверь установку

` + "```bash" + `
go version
` + "```" + `

Ты должен увидеть что-то вроде:

` + "```" + `
go version go1.22.4 darwin/arm64
` + "```" + `

---

## Настройка IDE

### VS Code (бесплатный, рекомендуем)

1. Скачай [VS Code](https://code.visualstudio.com/)
2. Открой VS Code
3. Установи расширение **Go** (от Go Team at Google)
4. VS Code предложит установить Go-инструменты — нажми **Install All**

### GoLand (платный, от JetBrains)

Если есть лицензия или студенческий доступ — GoLand отлично работает из коробки.

---

## Проверка

Создай файл и запусти:

` + "```bash" + `
mkdir hello && cd hello
go mod init hello
` + "```" + `

Создай файл ` + "`main.go`" + `:

` + "```go" + `
package main

import "fmt"

func main() {
    fmt.Println("Go установлен!")
}
` + "```" + `

Запусти:

` + "```bash" + `
go run main.go
` + "```" + `

Если видишь ` + "`Go установлен!`" + ` — всё готово. Переходим к первой программе.
`,

		"1-7-hello-world": `# Первая программа

Давай напишем первую программу на Go и разберём каждую строчку.

---

## Код

` + "```go" + `
package main

import "fmt"

func main() {
    fmt.Println("Hello, World!")
}
` + "```" + `

---

## Разбор по строкам

### ` + "`package main`" + `
Каждый файл Go принадлежит пакету. Пакет ` + "`main`" + ` — особенный: он говорит компилятору, что это исполняемая программа, а не библиотека.

### ` + "`import \"fmt\"`" + `
Подключаем пакет ` + "`fmt`" + ` (format) из стандартной библиотеки. Он содержит функции для вывода текста.

### ` + "`func main()`" + `
Точка входа — функция, с которой начинается выполнение программы. В Go она всегда называется ` + "`main`" + ` и находится в пакете ` + "`main`" + `.

### ` + "`fmt.Println(\"Hello, World!\")`" + `
Выводит текст в консоль и переходит на новую строку.

---

## Компиляция и запуск

### Запуск без компиляции
` + "```bash" + `
go run main.go
` + "```" + `
Компилирует и сразу запускает. Удобно для разработки.

### Компиляция в бинарник
` + "```bash" + `
go build -o myapp main.go
./myapp
` + "```" + `
Создаёт исполняемый файл. Его можно запустить на любой машине с той же ОС — без установки Go.

---

## Попробуй сам

1. Измени текст на ` + "`\"Привет, Go!\"`" + `
2. Запусти ` + "`go run main.go`" + `
3. Скомпилируй через ` + "`go build`" + ` и запусти бинарник

Поздравляю — ты написал свою первую программу на Go!
`,

		// ── Спринт 2. Фундамент инженерии ──

		"2-1-hardware": `# Аппаратное обеспечение

Прежде чем писать код, полезно понять, на чём он выполняется.

---

## Основные компоненты компьютера

### CPU (процессор)
Мозг компьютера. Выполняет инструкции — сложение, сравнение, переходы. Когда ты запускаешь ` + "`go run main.go`" + `, процессор выполняет твой код.

### RAM (оперативная память)
Быстрая временная память. Когда программа запущена, её переменные живут в RAM. При выключении всё стирается.

### HDD / SSD (диск)
Постоянное хранилище. Файлы, программы, базы данных — всё здесь. SSD в десятки раз быстрее HDD.

---

## Как код превращается в программу

` + "```" + `
main.go  →  компилятор Go  →  бинарник (машинный код)  →  CPU выполняет
` + "```" + `

1. Ты пишешь код на Go (текстовый файл)
2. Компилятор ` + "`go build`" + ` переводит его в машинный код
3. ОС загружает бинарник в RAM
4. CPU выполняет инструкции

Go компилируется **напрямую в машинный код** — без интерпретатора (как Python) или виртуальной машины (как Java). Поэтому Go-программы работают быстро.

---

## Почему это важно

- **RAM ограничена** — нельзя хранить бесконечно много данных в памяти
- **CPU считает быстро** — но плохой алгоритм всё равно будет медленным
- **Диск медленный** — обращение к файлу в 100 000 раз медленнее, чем к RAM
- **Сеть ещё медленнее** — запрос к другому серверу занимает миллисекунды

Хороший разработчик понимает, где живут его данные и сколько стоит каждая операция.
`,

		"2-2-how-internet-works": `# Как работает Интернет

Каждый раз, когда ты открываешь сайт, за кулисами происходит сложный процесс. Разберём его по шагам.

---

## IP-адреса

Каждый компьютер в сети имеет уникальный адрес — **IP-адрес**.

- ` + "`192.168.1.1`" + ` — локальный адрес (внутри твоей сети)
- ` + "`142.250.74.46`" + ` — публичный адрес (Google)
- ` + "`127.0.0.1`" + ` — localhost (твой собственный компьютер)

---

## Порты

На одном компьютере может работать много сервисов. **Порт** — это номер, который указывает, какому сервису предназначен запрос.

| Порт | Сервис |
|------|--------|
| 80 | HTTP (веб) |
| 443 | HTTPS (защищённый веб) |
| 5432 | PostgreSQL |
| 8080 | Часто используется для backend |

Адрес с портом: ` + "`localhost:8080`" + ` — твой компьютер, порт 8080.

---

## DNS

Люди не запоминают IP-адреса. **DNS** (Domain Name System) превращает имена в адреса:

` + "```" + `
google.com  →  DNS-сервер  →  142.250.74.46
` + "```" + `

Когда ты набираешь ` + "`google.com`" + ` в браузере:
1. Браузер спрашивает DNS: «Какой IP у google.com?»
2. DNS отвечает: ` + "`142.250.74.46`" + `
3. Браузер подключается к этому IP

---

## Полная картина

` + "```" + `
Ты набираешь: google.com
     ↓
DNS: google.com → 142.250.74.46
     ↓
Браузер → 142.250.74.46:443 (HTTPS)
     ↓
Сервер Google отдаёт HTML-страницу
     ↓
Браузер рисует страницу
` + "```" + `

Когда ты будешь писать свой сервер на Go, он будет слушать на определённом порту и отвечать на запросы — точно так же.
`,

		"2-3-network-protocols": `# Сетевые протоколы

Протокол — это набор правил, по которым компьютеры общаются друг с другом. Как язык общения.

---

## TCP (Transmission Control Protocol)

**Надёжный** протокол. Гарантирует доставку данных в правильном порядке.

Как работает:
1. Устанавливает соединение (рукопожатие)
2. Отправляет данные по частям
3. Получатель подтверждает каждую часть
4. Если часть потерялась — отправляет снова

Используется: HTTP, HTTPS, базы данных, email.

---

## UDP (User Datagram Protocol)

**Быстрый**, но ненадёжный. Не гарантирует доставку и порядок.

Как работает:
1. Отправляет данные без установки соединения
2. Не ждёт подтверждения
3. Если пакет потерялся — не переотправляет

Используется: видеозвонки, онлайн-игры, DNS.

---

## Сравнение

| | TCP | UDP |
|---|---|---|
| Надёжность | Гарантирует доставку | Может потерять данные |
| Скорость | Медленнее (ждёт подтверждения) | Быстрее (не ждёт) |
| Порядок | Гарантирует | Не гарантирует |
| Использование | Веб, API, БД | Видео, игры, стриминг |

---

## IP (Internet Protocol)

IP работает уровнем ниже — он отвечает за **адресацию** (куда отправить данные). TCP и UDP работают **поверх IP**.

Поэтому говорят **TCP/IP** — это связка: IP доставляет пакеты по адресу, а TCP гарантирует их целостность.

Твой Go-сервер будет работать по TCP/IP — каждый HTTP-запрос под капотом использует TCP.
`,

		"2-4-osi-model": `# Модель OSI

Модель OSI — это способ понять, как данные путешествуют по сети. Она делит процесс на **7 уровней**.

---

## Упрощённая версия

Тебе не нужно знать все 7 уровней наизусть. Достаточно понимать 4 ключевых:

| Уровень | Что делает | Пример |
|---------|-----------|--------|
| **Приложение** | Твой код, HTTP-запросы | Go-сервер, браузер |
| **Транспорт** | Надёжная доставка данных | TCP, UDP |
| **Сеть** | Маршрутизация между сетями | IP-адреса |
| **Физический** | Провода, Wi-Fi, сигналы | Ethernet-кабель |

---

## Как данные путешествуют

Когда ты отправляешь запрос к серверу:

` + "```" + `
Твой код: GET /api/weather
    ↓
HTTP → добавляет заголовки
    ↓
TCP → разбивает на пакеты, нумерует
    ↓
IP → добавляет адрес отправителя и получателя
    ↓
Физический → биты по проводу / Wi-Fi
    ↓
... интернет ...
    ↓
Сервер: обратный процесс снизу вверх
` + "```" + `

Каждый уровень добавляет свою «обёртку». На принимающей стороне обёртки снимаются в обратном порядке.

---

## Зачем это знать

Когда твой Go-сервер не отвечает на запросы, проблема может быть на любом уровне:

- **Приложение** — баг в коде
- **Транспорт** — порт занят
- **Сеть** — неправильный IP, firewall
- **Физический** — кабель отключён

Понимание уровней помогает быстрее находить причину проблемы.
`,

		"2-5-http-https": `# Протокол HTTP/HTTPS

HTTP — главный протокол веба. Каждый раз, когда ты открываешь сайт или вызываешь API, используется HTTP.

---

## HTTP-запрос

` + "```" + `
GET /api/users?page=1 HTTP/1.1
Host: example.com
Content-Type: application/json
Authorization: Bearer token123
` + "```" + `

Состоит из:
- **Метод** — что хотим сделать (GET, POST, PUT, DELETE)
- **Путь** — к какому ресурсу обращаемся
- **Заголовки** — метаданные запроса
- **Тело** — данные (для POST, PUT)

---

## HTTP-методы

| Метод | Действие | Пример |
|-------|----------|--------|
| GET | Получить данные | Список пользователей |
| POST | Создать | Новый пользователь |
| PUT | Обновить полностью | Изменить профиль |
| PATCH | Обновить частично | Изменить только имя |
| DELETE | Удалить | Удалить задачу |

---

## Статус-коды

Сервер отвечает с кодом, который говорит что произошло:

| Код | Значение |
|-----|----------|
| **200** | OK — всё хорошо |
| **201** | Created — ресурс создан |
| **400** | Bad Request — ошибка в запросе |
| **401** | Unauthorized — нужна авторизация |
| **404** | Not Found — ресурс не найден |
| **500** | Internal Server Error — ошибка на сервере |

---

## HTTPS

HTTPS = HTTP + **шифрование** (TLS/SSL). Данные шифруются между браузером и сервером. Третья сторона не может их прочитать.

Почти все современные сайты используют HTTPS. Если видишь замок в браузере — соединение зашифровано.

---

## Что это значит для тебя

Твой Go-сервер будет:
1. Принимать HTTP-запросы (GET, POST, PUT, DELETE)
2. Читать заголовки и тело запроса
3. Обрабатывать данные
4. Возвращать ответ со статус-кодом и JSON
`,

		"2-6-what-is-api": `# Что такое API

API (Application Programming Interface) — способ, которым программы общаются друг с другом.

---

## Простая аналогия

Представь ресторан:
- **Ты** — клиент (фронтенд)
- **Официант** — API
- **Кухня** — сервер (бекенд)

Ты не идёшь на кухню сам. Ты говоришь официанту что хочешь, он передаёт на кухню, кухня готовит и официант приносит результат.

API работает так же: фронтенд отправляет запрос, бекенд обрабатывает и возвращает данные.

---

## REST API

REST — самый популярный стиль API. Основные принципы:

- Ресурсы имеют **URL**: ` + "`/api/users`" + `, ` + "`/api/tasks/42`" + `
- Действия определяются **HTTP-методами**: GET, POST, PUT, DELETE
- Данные передаются в **JSON**
- Сервер **не хранит состояние** между запросами (stateless)

Пример REST API для задач:

| Действие | Метод | URL |
|----------|-------|-----|
| Все задачи | GET | /api/tasks |
| Одна задача | GET | /api/tasks/42 |
| Создать | POST | /api/tasks |
| Обновить | PUT | /api/tasks/42 |
| Удалить | DELETE | /api/tasks/42 |

---

## gRPC (кратко)

gRPC — альтернатива REST для общения между сервисами. Быстрее, строже типизирован. Используется внутри микросервисных архитектур. На этом курсе мы фокусируемся на REST.

---

## Как читать документацию API

Когда работаешь с чужим API (например, погодным сервисом), смотри:

1. **Base URL** — адрес сервера
2. **Endpoints** — какие пути доступны
3. **Параметры** — что передавать
4. **Ответ** — что вернётся
5. **Авторизация** — нужен ли ключ

На этом курсе ты научишься и **использовать** чужие API, и **создавать** свои.
`,

		"2-7-data-formats": `# Форматы данных

Когда сервер отвечает клиенту, данные нужно передать в определённом формате. Самый популярный — JSON.

---

## JSON (JavaScript Object Notation)

` + "```json" + `
{
  "id": 1,
  "name": "Алексей",
  "email": "alex@example.com",
  "age": 24,
  "isPremium": false,
  "skills": ["Go", "SQL", "Docker"],
  "address": {
    "city": "Москва",
    "country": "Россия"
  }
}
` + "```" + `

### Типы данных в JSON

| Тип | Пример |
|-----|--------|
| Строка | ` + "`\"hello\"`" + ` |
| Число | ` + "`42`" + `, ` + "`3.14`" + ` |
| Булево | ` + "`true`" + `, ` + "`false`" + ` |
| Null | ` + "`null`" + ` |
| Массив | ` + "`[1, 2, 3]`" + ` |
| Объект | ` + "`{\"key\": \"value\"}`" + ` |

---

## Почему JSON стал стандартом

- **Читаемый** — человек может прочитать и понять
- **Лёгкий** — меньше символов, чем XML
- **Универсальный** — поддерживается во всех языках
- **Структурированный** — вложенные объекты и массивы

---

## XML (для сравнения)

` + "```xml" + `
<user>
  <id>1</id>
  <name>Алексей</name>
  <email>alex@example.com</email>
</user>
` + "```" + `

XML более многословен и сегодня используется реже. В современных API почти всегда JSON.

---

## JSON в Go

В Go есть встроенный пакет ` + "`encoding/json`" + ` для работы с JSON. Ты будешь использовать его в каждом проекте:

` + "```go" + `
type User struct {
    ID    int    ` + "`json:\"id\"`" + `
    Name  string ` + "`json:\"name\"`" + `
    Email string ` + "`json:\"email\"`" + `
}
` + "```" + `

Мы подробно разберём это в модуле 4, когда будем создавать HTTP-сервер. Пока запомни: **JSON — основной формат обмена данными в вебе**.
`,
	}

	for slug, content := range lessons {
		db.Model(&models.Lesson{}).Where("slug = ?", slug).Update("content", content)
	}
}
